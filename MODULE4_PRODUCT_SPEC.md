# 模块 4：实时语音跟随产品与技术设计冻结

版本：4A-1.1

冻结日期：2026-08-16

适用平台：iPhone、iPad

最低系统版本：iOS 17

设计基线：`89b084d5fd028458531df79c9c565811ee6c9f87`

本文是 `PRODUCT_SPEC.md`“模块 4：实时语音跟随”的细化规格。若两者存在歧义，以本文对模块 4 的已冻结决定为准，但不得据此降低 `PRODUCT_SPEC.md` 的通用质量、隐私和验收要求。本文件只冻结产品和技术设计；模块 4A 不实现生产代码。

## 1. 用户目标与非目标

### 1.1 用户目标

实时语音跟随帮助中文口播用户在不保持固定语速的情况下阅读稿件：

1. 用户朗读当前句时，提词位置按句子推进。
2. 用户停顿时，文本位置保持不动。
3. 用户漏少量词、重复当前句或上一句、使用已支持的常见等价表达时，不发生错误回退或远距离跳转。
4. 用户短暂稿外发挥后，系统保持原位置；用户回到稿件后，系统能够以高置信度重新定位。
5. 用户可随时切换回固定速度提词，且保留当前全局 `Character` 阅读锚点。
6. 识别权限、设备能力或运行时错误不得阻塞固定速度提词，也不得损坏或错误停止正常视频录制。
7. 所有语音识别只在设备端执行，不上传或持久化语音识别内容。

### 1.2 非目标

首版明确不实现：

- 逐字卡拉 OK 动画、声纹识别、发音评分、情绪或表现分析。
- 任意同义改写或开放域语义理解；首版只支持确定性文本相似度和版本化本地等价词表。
- 识别文本编辑器、识别历史、识别音频回放或识别音频文件。
- 联网语音识别、云端转写、账户同步或远程日志。
- 自动字幕、分段重拍、片段拼接或模块 5 及后续功能。
- `SpeechAnalyzer`、`SpeechTranscriber` 或其他仅新系统可用的 API 作为生产基础。
- StoreKit、付费墙、额度扣减或购买流程。免费/Pro 只保留协议边界。

## 2. 已冻结的产品决定

### 2.1 设备端识别与隐私

以下决定不可由实现阶段自行放宽：

1. 首版只允许设备端语音识别。
2. 每个 `SFSpeechRecognitionRequest` 的 `requiresOnDeviceRecognition` 必须显式设置为 `true`。
3. 只有目标语言的 `SFSpeechRecognizer.supportsOnDeviceRecognition == true` 时，语音跟随才可启用。
4. 不允许在本地识别不可用、临时失败或飞行模式下自动回退到联网识别。
5. 不持久化识别文本、识别候选、识别时间线、匹配评分、临时锚点或专供识别的音频副本。单次识别任务期间允许这些处理数据短暂存在于易失内存，以完成匹配和状态转换。
6. 处理所必需的 PCM 只可短暂存在内存；跨识别任务桥接的环形缓冲硬上限为 2 秒。识别任务结束或取消、音频中断、模式切换、页面退出或代次失效时，必须释放内存候选、评分、临时锚点和 PCM。上述内容不得写入 SwiftData、UserDefaults、文件、日志或分析事件。
7. 不记录稿件正文、识别文本、音频内容、完整本地路径、系统语言模型内容或用户可识别信息。
8. 本地识别不支持时，固定速度提词和正常视频录制继续可用。
9. 录制视频中的正常 AAC 音轨属于用户主动录制的媒体，不是“语音识别音频副本”；其安全规则继续由模块 3 管理。

### 2.2 系统版本与框架

- 最低实现使用 iOS 17 可用的 `SFSpeechRecognizer`、`SFSpeechAudioBufferRecognitionRequest` 和 `SFSpeechRecognitionTask` 抽象。
- 能力检查必须同时覆盖：目标 locale 可构造、授权状态、`supportsOnDeviceRecognition`、运行时可用状态和可用音频输入。
- `SpeechAnalyzer` 可在未来通过新的适配器实现，但不得进入首版的最低依赖、公共领域模型或 iOS 17 编译路径。
- 未来适配器必须满足相同的设备端、无持久化、无内容日志和可取消协议，且需单独产品审核。

### 2.3 首发语言

- 首发识别 locale 为 `zh-CN`，验收覆盖普通话夹常见英文单词、阿拉伯数字和常见中英文标点。
- 不承诺粤语、方言、纯英文长稿或自动语言检测。
- 简繁体不做自动互转；原稿范围映射必须保持不变。

### 2.4 免费/Pro 边界

- `SpeechTracking` 只依赖抽象的能力/权益查询和用量事件协议，不依赖 StoreKit 或 Subscription UI。
- 本阶段不决定免费次数、计量单位、重置周期或 Pro 价格，不显示付费墙，不阻止已获本地能力支持的开发/验收流程。
- 额度和购买语义仍由 `SPEC-006` 约束，在模块 8 决策前不得写死。

## 3. 固定速度与语音跟随模式

### 3.1 模式事实来源

模式使用单一枚举表示：

- `fixedSpeed`：沿用模块 2 的 `TeleprompterPlaybackMachine`、单调时间和逻辑点/秒。
- `speechTracking`：识别和匹配组件只产生 `ScriptReadingAnchor` 提议；最终滚动、布局和持久化仍由现有 `TeleprompterViewModel`、`TeleprompterPlaybackMachine` 和虚拟化文本视图负责。

不得创建第二套像素滚动状态、第二个阅读位置事实来源或绕过现有全局 `Character` 锚点。

### 3.2 切换规则

| 操作 | 冻结行为 |
|---|---|
| 固定速度运行中切到语音跟随 | 立即在当前锚点暂停固定速度；检查能力和权限；倒计时结束且设备端识别真正就绪后进入监听。准备期间不继续滚动。 |
| 固定速度暂停时切到语音跟随 | 保留当前锚点；成功准备后监听，失败时保持固定速度暂停。 |
| 语音跟随切回固定速度 | 取消识别任务和音频订阅，使旧结果失效；保留最后确认锚点；进入固定速度暂停，用户明确点击后才继续。 |
| 语音跟随不可用或失败 | 锚点保持；界面切回可用的固定速度暂停状态并说明原因，不自动开始滚动。 |
| 页面退出或进入后台 | 停止语音识别、清除内存候选和音频缓冲、保存最后确认锚点；返回后不自动重新监听。 |
| 稿件在跟随期间变化 | 使当前索引和识别代次失效，保持已确认锚点并显示“稿件已更新，请重新开始语音跟随”；不得用旧索引推进。 |

模式偏好可以作为用户默认设置保存，但进入页面不得仅因上次选择了语音跟随就自动申请权限或自动打开麦克风。每次监听均由用户明确开始。

## 4. 权限与能力门控

### 4.1 请求时机

App 启动、稿件编辑、固定速度提词和仅进入普通提词页面时不得请求语音识别权限。

启用与首次请求顺序：

1. 用户主动选择“语音跟随”并点击开始。
2. 创建 `zh-CN` `SFSpeechRecognizer`，在请求任何新权限前检查 `supportsOnDeviceRecognition`。
3. 若不支持设备端识别，不请求 Speech 或麦克风权限，直接显示本地能力不可用并保留固定速度模式与正常录像。
4. 若支持设备端识别，先展示用途说明：“语音只在本机用于定位稿件，不持久化识别文本或识别音频。”
5. 请求 Speech Recognition 权限；只有 Speech 权限允许后，才请求尚未取得的麦克风权限。
6. 授权完成后重新检查同一 locale 的 `supportsOnDeviceRecognition` 与 `isAvailable`；任一条件不满足都不得启动识别。
7. 每次真正创建 `SFSpeechRecognitionRequest` 时显式设置 `requiresOnDeviceRecognition = true`，并在启动任务前验证该值未被适配器覆盖。
8. 上述任一阶段失败、不可用或能力变化时都不得回退到联网识别。

摄像提词不因普通视频录制自动请求 Speech 权限。只有用户主动启用语音跟随时才请求 Speech 权限；既有摄像头、麦克风权限仍由模块 3 流程管理。

### 4.2 权限与能力结果

| 条件 | 用户结果 |
|---|---|
| Speech 未决定 | 仅在权限前能力检查确认支持设备端识别、并展示用途说明后，请求一次系统权限。 |
| Speech 拒绝/受限 | 显示原因和系统设置入口；固定速度与录制可用；重入不循环弹窗。 |
| 麦克风拒绝/受限 | 不启动识别；显示原因和设置入口；摄像录制继续遵守模块 3“不得生成误认为有声的正常视频”规则。 |
| `supportsOnDeviceRecognition == false` | 显示“此设备当前不支持设备端语音识别”；不得请求 Speech 或麦克风权限，不得创建可能联网的任务。 |
| recognizer 暂不可用 | 保持锚点并提供“重新检查”；不得自动改用网络。 |
| 飞行模式且设备端能力可用 | 语音跟随应正常工作。 |
| 飞行模式且本地资源不可用 | 明确降级为固定速度；不得提示网络重试能自动解决或发起网络识别。 |

## 5. 音频来源与所有权

### 5.1 单一采集所有者

任何时刻同一物理输入只能有一个音频采集所有者。语音识别只能订阅该所有者提供的短生命周期 PCM 流，不能独立争抢或重置正在录制的 `AVAudioSession`。

| 场景 | 音频来源 | 所有权规则 |
|---|---|---|
| 普通提词 | iOS 17 兼容的普通提词音频源，计划由隔离服务管理 `AVAudioEngine` 输入 tap | 仅语音跟随运行时激活；停止后移除 tap、停止引擎并释放缓冲。 |
| 摄像提词、未录制 | 现有 `AVCaptureSession` 的同一麦克风输入，通过受控 PCM 分发边界提供 | 不创建第二个 `AVAudioEngine` 或平行 Capture Session。 |
| 摄像提词、正在录制 | 与 Movie Output 相同的麦克风、路由和 Capture Session；计划使用同一音频输入上的受控 `AVCaptureAudioDataOutput` 分发瞬时样本 | 识别只作为观察者；失败或取消只能退订识别，不能停止录像、移除录音输入或停用录制音频会话。 |

CameraRecording 继续拥有录制音频会话、路由、中断和文件音轨事实；SpeechTracking 不得调用停止录像、完成文件或照片保存接口。

### 5.2 路由和格式

- 使用系统当前实际输入，包括内置麦克风和可用 Bluetooth HFP 输入；不硬编码设备型号。
- PCM 转换在非主线程的隔离执行上下文完成；只转换识别所需格式，不改变 Movie Output 的 AAC 48 kHz 配置。
- 路由改变时使当前识别任务代次失效并进入可恢复中断。普通提词由用户明确重新开始；摄像提词遵守模块 3 的录制/中断状态，不自动续录。
- 识别服务不得选择与摄像录制 UI 显示不一致的隐藏输入。

### 5.3 长时间任务轮换

50 秒是 TakeFlow 为控制资源、取消边界和长时间稳定性而制定的内部主动轮换策略，不是 Apple 声明的固定任务时限。识别协调器在自然句末或内部任务运行达到 50 秒时轮换 `SFSpeechRecognitionTask`。轮换只保留最多 2 秒的易失内存 PCM，用于避免句边界丢失；新旧结果以 recognition generation、音频时间范围和已确认文本范围去重。轮换期间不得丢失已确认锚点、重复推进、回退、写入音频文件或产生超过 2 秒的无界缓冲，也不得改变录像状态。

## 6. 稿件拆分与规范化

### 6.1 不可变追踪文档

进入语音跟随前，在后台从当前正式稿件版本构建不可变 `SpeechTrackingDocument`。每个追踪单元至少包含：

- 稳定的单元序号。
- 原文全局 `Character` 范围。
- 规范化 token 序列。
- 前后相邻单元。
- 内容 revision/hash。
- 用于候选检索的本地 n-gram 索引键。

原文范围必须使用 Swift 扩展字素簇安全边界；像素和 UTF-16 偏移不得成为持久化事实。

### 6.2 句子拆分

1. `。！？!?；;` 和换行是硬边界。
2. `，,、：:` 是软边界；仅当一个硬句的规范化长度超过 42 个 token 时用于拆分。
3. 单个追踪单元目标为 8–32 个 token，硬上限 48；超过上限时优先在软标点拆分，其次在安全 token/`Character` 边界拆分。
4. 少于 4 个 token 的片段与相邻片段合并，但不得跨段落硬换行，也不得超过 48 个 token。
5. Emoji 和仅含标点的片段不单独成为可追踪句；其原文范围归入相邻单元。
6. 空稿件或规范化后无可读 token 时不得启动语音跟随。

### 6.3 规范化

规范化必须同时保留规范 token 到原文 `Character` 范围的映射：

- 使用 Unicode 兼容等价规范化统一全角/半角和组合形式，但不修改原稿。
- 英文字母使用稳定、locale 明确的大小写折叠；连续拉丁字母作为一个 token。
- 连续阿拉伯数字作为一个 token；全角数字转为半角。
- 对明确无歧义的中文整数表达和首版批准的英文数字词执行版本化本地规范化；模糊表达不强制转换。
- 句子拆分后，空白和常见标点不参与相似度，但仍保留原文范围映射。
- 中文以扩展字素簇为基础 token；英文、数字按词 token。
- 不自动简繁转换，不调用网络词典，不使用云端 embedding。
- 常见等价表达只来自随 App 版本发布、可测试、可审核的本地词表；不得运行时下载或从用户语音学习。

## 7. 匹配、置信度与重新定位

### 7.1 候选窗口

正常跟随只计算“当前单元前 2 个、当前单元及后 6 个”的有界窗口。候选检索不得对每个 partial result 全文扫描。

当进入重新定位时，使用构建阶段生成的 n-gram 倒排索引取得最多 8 个候选，再计算完整相似度。10 万字符稿件跳到 25%、50%、75% 或结尾附近不得从开头同步遍历布局或全文逐句比对。

### 7.2 分数

每个候选分数范围为 0–1，由确定性组件组成：

- 规范化编辑相似度：50%。
- 有序 token 覆盖率：30%。
- token/bigram Dice 相似度：20%。

系统 Speech 置信度只可作为次级门控，不能单独决定推进；缺失置信度时仍使用上述确定性分数。

### 7.3 推进规则

- 高置信推进：最佳候选分数 `>= 0.82`，且比第二候选至少高 `0.10`；需要一个 final result，或两个间隔至少 150 ms、指向同一候选的连续 partial result。
- 有漏词的当前句：有序覆盖率 `>= 0.75`、综合分数 `>= 0.78`、候选领先 `>= 0.10` 时可推进。
- 中置信保持：分数位于 `[0.70, 0.82)`，只有连续两个 final result 指向同一候选且领先 `>= 0.10` 才推进；否则保持。
- 低置信保持：分数 `< 0.70`、领先 `< 0.08` 或候选冲突时，锚点不得变化。
- 正常模式一次最多向前推进 2 个单元；更远候选必须进入重新定位规则。
- 自动匹配不得向后移动锚点。重复当前句或前一句只保持位置，不回退、不远跳。
- 稿件有重复句时，位置距离优先；若最佳与次佳分数差 `< 0.10`，保持当前位置等待更多上下文。

### 7.4 稿外内容与重新定位

- 连续 2 个 final result 低于阈值，或连续 3 秒只有低置信输入时进入 `relocalizing`；锚点保持。
- 远距离候选必须位于当前锚点之后，分数 `>= 0.88`、领先 `>= 0.12`，并由两个连续结果确认；其中至少一个必须为 final result。
- 确认后一次跳转到目标单元起始 `Character` 锚点，并产生一次可访问状态更新。
- 任何自动向后重新定位均禁止；用户要回到前文必须手动拖动或使用位置控制。
- 重新定位超过 10 秒仍无高置信候选时进入“未找到稿件位置”保持状态，提供“继续聆听”“手动定位”“切换固定速度”。

### 7.5 用户手动调整

用户开始拖动时：

1. 语音跟随进入 `userAdjusting`，停止应用识别锚点。
2. 拖动结束后，以最终 `ScriptReadingAnchor` 所在追踪单元作为新中心；落在单元间隙时选择后一个可读单元。
3. 增加 matching generation，丢弃拖动开始前产生的所有 partial/final 结果和候选任务。
4. 清空已累计的低置信和重复上下文；新语音结果不得把位置拉回旧锚点。
5. 用户明确继续后才恢复监听；不自动开始固定速度滚动。

## 8. 状态机与恢复

### 8.1 语音跟随状态

语音跟随拥有独立的识别状态机，但不复制提词滚动状态机：

- `inactive`
- `checkingCapability`
- `requestingAuthorization`
- `preparingAudio`
- `listening`
- `holdingLowConfidence`
- `relocalizing`
- `userAdjusting`
- `paused`
- `interrupted(reason)`
- `unavailable(reason)`
- `failed(error, recovery)`
- `stopping`
- `finished`

所有事件携带 tracking session UUID、audio source UUID、document revision 和 generation；旧任务、旧音频源或旧稿件结果不得更新新生命周期。

### 8.2 关键转换

| 当前状态/事件 | 结果 |
|---|---|
| inactive + 用户开始 | checkingCapability；能力满足后才请求缺失权限。 |
| 权限允许 + 本地能力支持 | preparingAudio；真实音频源就绪后 listening。 |
| 倒计时 | 复用现有倒计时；倒计时期间不向识别器投递用户音频，取消后回到 paused。 |
| listening + 高置信匹配 | 提交锚点提议；仍保持 listening。 |
| listening + 低置信 | holdingLowConfidence；锚点不变。 |
| 连续稿外输入 | relocalizing；满足远距离门控才跳转。 |
| 任意活动状态 + 用户拖动 | userAdjusting；取消旧匹配 generation。 |
| 音频中断/路由失效 | interrupted；停止识别并清理缓冲，不自动恢复。 |
| 识别任务错误 | 锚点保持；可恢复错误提供重试，不可恢复能力错误降级 fixedSpeed paused。 |
| 页面退出/后台 | stopping → inactive；释放请求、任务、tap、流、候选和缓冲。 |
| 文档变化 | failed(scriptChanged)；旧索引失效，必须重新加载。 |
| 最后单元确认 | finished；保存末尾锚点，不继续创建识别任务。 |

### 8.3 错误分类

至少区分：

- Speech 权限拒绝或受限。
- 麦克风权限拒绝或受限。
- `zh-CN` recognizer 不存在。
- 本地识别不支持。
- recognizer 暂不可用。
- 音频输入或路由不可用。
- 音频会话被中断。
- 识别任务失败或被系统取消。
- 音频转换失败。
- 稿件为空、稿件变化或索引失败。
- 启动超时、停止超时和内部状态冲突。

错误必须映射为可操作状态，禁止只显示“未知错误”。重试必须创建新的 session/generation 和识别任务，不能只改 UI；不可恢复错误必须保留固定速度入口。

### 8.4 摄像录制隔离

- SpeechTracking 失败、低置信、重定位失败或用户切回固定速度时，正在进行的正常录像继续。
- SpeechTracking 不得直接驱动 `RecordingStateMachine`，也不得完成、删除或重命名视频文件。
- 如果底层真实音频路由中断同时影响录制，模块 3 的 interruption episode、封口和手动重新准备规则是唯一事实来源；语音跟随只反映该结果。
- 录制期间不得为了重启识别而重启 Capture Session、替换麦克风输入或停用 `AVAudioSession`。

## 9. UI、文案与无障碍

### 9.1 UI 状态

普通提词和摄像提词都提供“固定速度 / 语音跟随”模式控制。必须显示：

- 当前模式。
- 是否正在监听。
- 当前为跟随、低置信保持、重新定位、中断或不可用。
- 切换固定速度、暂停/继续、手动定位和可用时重试入口。
- 摄像录制状态与语音跟随状态分开呈现，避免把“识别失败”显示成“录像失败”。

### 9.2 冻结中文文案

| 状态 | 文案 |
|---|---|
| 能力检查 | 正在检查设备端语音识别… |
| 准备音频 | 正在准备语音跟随… |
| 监听 | 正在听，请开始朗读 |
| 跟随 | 已跟随到第 %d 句 |
| 低置信 | 暂未听清，提词位置已保持 |
| 重新定位 | 正在重新定位稿件… |
| 未定位 | 暂未找到稿件位置；你可以继续朗读、手动定位或切换固定速度 |
| 本地不支持 | 此设备当前不支持设备端语音识别，固定速度提词仍可使用 |
| Speech 拒绝 | 未开启语音识别权限；固定速度提词仍可使用 |
| 麦克风拒绝 | 未开启麦克风权限，无法使用语音跟随 |
| 识别中断 | 语音跟随已中断，提词位置已保持 |
| 摄像录制仍正常 | 语音跟随不可用，视频仍在录制 |
| 稿件变化 | 稿件已更新，请重新开始语音跟随 |

最终实现允许为语法和本地化调整占位符，但不得改变“不上传、不自动联网降级、固定速度可用、锚点保持、录像独立”的含义。

### 9.3 VoiceOver

- 模式控件提供标签“提词模式”、值“固定速度”或“语音跟随”、切换后的结果提示。
- 开始、暂停、继续、重试、手动定位和切回固定速度均有独立标签、值和提示，触控区域至少 44×44 pt。
- 只在状态类别改变、权限/错误出现、完成重新定位时公告；不得朗读识别文本，不得每个 partial result 或每句重复打断用户。
- 同类状态公告最短间隔 2 秒；权限和不可恢复错误不受节流影响。
- 镜像只影响提词文字，不镜像无障碍控件或焦点顺序。

### 9.4 最大字体与布局

- 最大辅助功能字体下，退出、模式切换、开始/暂停、录像按钮和错误恢复入口必须位于安全区且可点击。
- 状态说明允许多行和滚动，不能遮挡提词正文、摄像预览关键区域、恢复片段卡片或底部录制控制。
- iPhone 竖屏、左右横屏和 iPad 均使用自适应布局，不写死单一设备坐标。
- 不得通过关闭 Dynamic Type、截断错误说明或缩小提词正文来取得布局通过。

## 10. 性能、资源与日志

### 10.1 自动化硬阈值

测量必须记录 Xcode、OS、模拟器/设备、Debug/Release 和语料 hash。测试超过阈值必须失败，不能只打印警告。

| 项目 | 1 万字符 | 10 万字符 |
|---|---:|---:|
| 后台拆句、规范化和索引建立 | <= 500 ms | <= 2,000 ms |
| 单次本地窗口匹配 p95（1,000 次） | <= 20 ms | <= 30 ms |
| 单次全局重新定位候选检索加评分 p95（200 次） | <= 40 ms | <= 60 ms |
| 主线程单次阻塞 | <= 16 ms | <= 16 ms |
| 额外索引峰值内存 | <= 8 MB | <= 32 MB |

上述为当前工具链的内部防退化阈值，不替代真机。iPhone 16 真机验收阈值为：1 万字符初始化 <= 500 ms、10 万字符初始化 <= 2,000 ms、10 万字符单次匹配 p95 <= 50 ms、锚点从匹配确认到 UI 接收 p95 <= 100 ms；每项至少运行 3 轮。

### 10.2 运行边界

- 规范化和索引在后台 actor/任务执行；主线程只接收小型状态和锚点。
- 候选集合有界，缓存有明确上限；不得随识别时长或稿件长度无限增长。
- partial result 不得触发全文 `String.count`、全文编辑距离、整篇 TextKit 赋值或布局。
- 切换模式、页面退出、音频中断和文档变化必须取消任务并结束流；continuation 不得悬挂。
- 连续 30 分钟真机跟随记录 CPU、内存、热状态和电量变化，但不得记录内容。

### 10.3 日志允许范围

允许记录：状态类别、错误类别、权限类别、能力布尔值、截短 session/generation 标识、候选数量、分数区间、句子序号、耗时和内存计数。

禁止记录：稿件正文、规范化正文、识别文本、候选文本、音频、完整路径、完整 UUID、联系人、设备名称中的用户自定义隐私内容或可关联用户的稳定标识。Release 不包含测试入口、Fake 音频、测试语料或调试面板。

## 11. 可量化验收矩阵

### 11.1 A 层：确定性算法语料验收

A 层只使用冻结且版本化的识别文本流，不连接真实麦克风、Speech Framework 或系统语言模型。所有包含预期位置的用例，其最终及中间 `Character` 锚点必须 100% 等于冻结预期；错误回退、错误远跳和旧结果覆盖手动锚点必须全部为 0。任何一项不满足即失败，不能引用真机识别波动降低算法层阈值。

| ID | 场景与固定输入 | 通过标准 |
|---|---|---|
| A-01 | 100 句完全照稿，逐句注入 final result | 100/100 按顺序推进；错误回退 0 次；错误远跳 0 次；最终锚点位于末句范围。 |
| A-02 | 30 句分别删除 1–20% token，保持原顺序 | 30/30 匹配目标句；单次最多前进 2 个单元；错误候选 0 次。 |
| A-03 | 当前句或上一句重复两遍，共 30 组 | 锚点回退 0 次；跨过未读句 0 次；重复期间最多保持当前位置。 |
| A-04 | 在 20 个位置分别停顿 2、5、10 秒且无识别结果 | 60/60 停顿期间 `Character` 锚点完全不变。 |
| A-05 | 20 组 5–15 秒稿外文本，随后朗读原稿连续两句 | 稿外阶段错误推进 0 次；恢复后不超过 2 个连续确认结果定位正确；错误远跳 0 次。 |
| A-06 | 从 10 万字符稿的 25%、50%、75% 和结尾前两个单元重新朗读 | 4/4 在两个确认结果内定位；不执行逐句全文扫描；结果锚点等于目标单元起始 `Character`。 |
| A-07 | 含 10 组重复句的稿件 | 每组均优先当前位置窗口；分差不足时保持；错误选择远端重复句 0 次。 |
| A-08 | 60 条普通话夹英文、阿拉伯数字和中英文标点的冻结语料 | 注入规范转写时 60/60 到达正确句；token 到原文 `Character` 范围往返 60/60 一致。 |
| A-09 | 在 50 个锚点手动向前/向后拖动，并注入旧 generation 结果 | 50/50 以新锚点重建窗口；旧结果写回 0 次；不自动回到拖动前位置。 |
| A-10 | 最佳分数 `<0.70` 或领先 `<0.08` 的 100 组歧义输入 | 100/100 保持锚点；不得为提高推进率放宽门控。 |
| A-11 | 正常任务轮换 20 次、重复回调和旧代次回调各 20 次 | 每次只有一个活动任务；重复推进 0 次；旧回调状态写回 0 次；内存缓冲不超过 2 秒。 |
| A-12 | 权限拒绝、本地不支持、任务错误、音频错误和稿件变化 | 每个分支进入指定错误/降级状态；固定速度入口存在；无悬挂任务或 continuation。 |
| A-13 | 30 条来自版本化批准词表的等价表达，以及 30 条未批准的近义改写 | 批准表达 30/30 映射到目标 token；未批准表达不得仅凭词表获得额外分数，低于门控时 30/30 保持锚点；测试记录词表版本。 |

### 11.2 UI 自动化

1. 固定速度运行中切到语音跟随，观察暂停、准备、监听，再切回固定速度暂停。
2. 权限拒绝、本地不支持和识别错误均显示冻结文案及固定速度入口，且不循环请求。
3. 低置信、重新定位、手动拖动和稿件变化时，UI 状态与锚点一致。
4. 普通提词和摄像提词分别完成模式切换；摄像录制中注入识别失败，录像 UI 保持 recording 并可正常停止/预览。
5. 最大辅助字体下，iPhone 竖屏/横屏和 iPad 的退出、模式、开始/暂停、录制及恢复入口均 `exists == true` 且 `isHittable == true`。
6. VoiceOver 标签、值、提示和错误状态可读取，且不暴露注入的识别文本。

### 11.3 B 层：真机设备端识别验收

B 层使用真实麦克风、真实 `zh-CN` 设备端识别能力及指定设备环境，衡量系统转写波动后的端到端结果。现有 48/50、45/50、18/20、9/10、27/30 等阈值是独立真机门槛，不得因一次失败而降低，也不得用 B 层容差替代 A 层 100% Character 锚点要求。

| ID | 场景 | 通过标准 |
|---|---|---|
| D-01 | iPhone 上在安静环境和校准为 65–70 dBA 的背景噪声中，分别完全照稿朗读冻结 50 句语料，各连续 3 轮 | 安静环境每轮至少 48/50、噪声环境每轮至少 45/50 句在读完该句后的 2 个识别结果内正确推进；两种环境错误回退和错误远跳均为 0。 |
| D-02 | 20 句分别漏 1–20% 词，以及批准词表中的 10 条等价表达，各连续 3 轮 | 漏词每轮至少 18/20、批准等价表达每轮至少 9/10 正确匹配；错误远跳为 0；记录词表版本。 |
| D-03 | 重复、2/5/10 秒停顿和 5–15 秒稿外发挥各 10 组 | 重复与停顿期间错误推进 0 次；稿外阶段错误推进 0 次；回稿后至少 9/10 在连续两句内重新定位。 |
| D-04 | 普通话夹英文、数字和标点冻结 30 句，连续 3 轮 | 每轮至少 27/30 正确匹配；错误回退和远跳均为 0。 |
| D-05 | Speech/麦克风首次允许、拒绝、设置恢复 | 请求时机与顺序正确；拒绝后固定速度可用且无循环弹窗；恢复后仅由用户开始监听。 |
| D-06 | `supportsOnDeviceRecognition == false` 的设备/locale | 3/3 不启动识别、不发起网络回退，固定速度和录像可用。若当前设备无法形成该能力，使用自动化证据并标记真机未验证。 |
| D-07 | 飞行模式、Wi-Fi 和蜂窝关闭 | 本地能力已安装时连续 3/3 可跟随；能力不可用时连续 3/3 明确降级且无网络错误循环。 |
| D-08 | 内置麦克风与一款 Bluetooth HFP 输入各连续 3 轮 | UI 显示实际路由；两种路由均满足 D-01 的错误跳转为 0；切换路由后不自动续听，用户重启后恢复。 |
| D-09 | 普通提词中音频中断、锁屏、后台和返回 | 每种场景 3/3 停止识别、保持锚点、释放麦克风；返回后不自动监听。 |
| D-10 | 摄像录制 30 秒时注入/触发识别失败 | 连续 5/5 录像继续计时并可手动停止；文件均可播放、有声音、无覆盖；识别错误与录像状态分开显示。 |
| D-11 | 摄像录制中 Bluetooth 路由变化或系统音频中断 | 模块 3 负责安全状态和文件封口；SpeechTracking 不产生第二个会话、不自动恢复录像。每个可触发场景 3/3。 |
| D-12 | 1 万/10 万字符初始化及 1,000 次匹配 | 满足第 10.1 节真机阈值，连续 3 轮；记录设备、系统、构建和语料 hash。 |
| D-13 | 最大字体与 VoiceOver，iPhone 竖/横屏和真实 iPad | 所有关键控件触控区域 >=44×44 pt；无裁剪/遮挡；完整流程可操作；焦点不进入已关闭页面；3/3。 |
| D-14 | 连续 30 分钟语音跟随 | 不出现第二套滚动、无限缓存、崩溃或强制结束；识别任务轮换后仍可推进；记录峰值内存、CPU、热状态和电量变化。 |

## 12. 协议与依赖边界

后续实现至少使用以下职责边界，命名可在代码评审时按项目规范调整：

- `SpeechRecognitionAuthorizing`：Speech 权限读取和请求。
- `OnDeviceSpeechRecognizing`：能力检查、启动、停止、结果流和错误；生产请求强制 on-device。
- `SpeechAudioSource`：普通提词或摄像提词的 PCM 流；暴露 source/session generation，不暴露文件。
- `SpeechTrackingDocumentBuilding`：拆句、规范化、原文范围映射和索引。
- `SpeechPositionMatching`：有界候选、评分、低置信和重定位。
- `SpeechTrackingCoordinating`：识别状态机、代次、取消和锚点提议。
- `VoiceTrackingAccessChecking`：免费/Pro 能力协议；本模块无 StoreKit 实现。
- `VoiceTrackingUsageObserving`：可选的非内容型会话开始/结束事件协议；本模块不持久化、不扣额度。

`SpeechTracking` 可以依赖 `Core` 和稳定的 `ScriptReadingAnchor`；`Teleprompter`、`CameraRecording` 通过协议组合 SpeechTracking。SpeechTracking 不得读取 SwiftData 实现、SwiftUI View、视频文件目录或 Subscription 内部状态。

## 13. 模块 4B–4H 开发计划

每个阶段必须独立构建、测试并更新状态；不得用后续阶段代码使当前阶段验收通过。

### 4B：追踪文档、规范化与协议骨架

**输入**：正式稿件字符串、内容 revision、`ScriptReadingAnchor`、本文第 2、6、10、12 节。

**输出**：Sendable 领域模型；安全句子拆分；规范 token/原文 Character 双向映射；有界索引；识别、音频、权益协议但无系统实现。

**允许文件**：`TakeFlow/SpeechTracking/**`、必要的 `TakeFlow/Core/Models/**` 小型值类型、`TakeFlowTests/SpeechTracking*Tests.swift`、模块文档。不得修改 Teleprompter、CameraRecording、权限或工程配置。

**单元测试**：硬/软边界、Emoji/组合字符、超长段、空稿、中英文数字标点、双向范围、内容 revision、1 万/10 万初始化、内存上限。

**UI/真机**：无 UI；无需真机通过，但性能数据必须记录模拟器/工具链。

**完成定义**：领域和性能测试全部通过；不存在 Speech/AVFoundation/StoreKit import；未建立生产识别任务。

**禁止提前实现**：匹配状态机、权限请求、音频采集、UI、摄像整合。

### 4C：匹配器与语音跟随状态机

**输入**：4B 文档索引和注入的不可变识别假设。

**输出**：候选评分、推进、重复保持、低置信、稿外保持、全局重定位、手动锚点 generation 和明确状态机。

**允许文件**：`TakeFlow/SpeechTracking/**` 纯领域实现、`TakeFlowTests/SpeechTracking*Tests.swift`、状态文档。

**单元测试**：A-01 至 A-13 全部硬断言；两种事件顺序、取消、重复、迟到 generation；1,000 次本地匹配和 200 次重定位性能。

**UI/真机**：无生产 UI；可用测试 harness，但不得进入 App Target。

**完成定义**：固定语料结果和阈值全部通过，失败测试会真实失败；不依赖 Speech、麦克风或网络。

**禁止提前实现**：系统权限、真实识别、普通/摄像页面集成、付费墙。

### 4D：iOS 17 设备端识别与普通提词音频服务

**输入**：4C 状态机、权限协议、普通提词音频源协议。

**输出**：`SFSpeechRecognizer` iOS 17 适配器；强制 `requiresOnDeviceRecognition`；能力门控；普通提词的隔离音频源；任务轮换和 2 秒内存桥接。

**允许文件**：`TakeFlow/SpeechTracking/**`、`TakeFlow/Core/Permissions/**`、依赖组装所需的 `TakeFlow/App/**`；只有确需加入 Speech 权限用途说明时才允许精确修改 `TakeFlow.xcodeproj/project.pbxproj`，不得改 Bundle ID、Team、签名或其他权限。测试位于 `TakeFlowTests/**`。

**单元测试**：授权四态、本地支持/不支持、请求强制 on-device、无网络回退、任务轮换、音频源取消、路由/中断、旧回调隔离。

**UI/真机**：定向真机验证权限和飞行模式服务边界；此阶段不声称完成最终提词 UI。

**完成定义**：iOS 17 Generic Device Debug/Release 编译通过；Release 无 Fake/测试入口；抓包/网络审计证明模块 4 未创建网络客户端。

**禁止提前实现**：Teleprompter UI、Camera Capture 音频分发、StoreKit、模块 5。

### 4E：普通提词器集成

**输入**：4D 识别服务、4C 锚点提议、模块 2 唯一滚动状态机。

**输出**：普通提词的模式切换、倒计时、状态/错误/恢复 UI、偏好保存、手动拖动锚点重置。

**允许文件**：`TakeFlow/Teleprompter/**`、`TakeFlow/SpeechTracking/**`、必要的 `TakeFlow/App/**` 依赖组装、对应单元/UI 测试和文档。不得修改 CameraRecording。

**单元测试**：模式转换、锚点所有权、文档变化、前后台、错误降级、持久化不改正文、模块 1/2 全回归。

**UI/真机**：普通提词权限、模式、完全照稿/漏词/重复/停顿/稿外/手动定位、飞行模式、最大字体和 VoiceOver。

**完成定义**：普通提词 D-01 至 D-09 中适用项目通过；固定速度全部回归通过；语音失败不破坏现有偏好和锚点。

**禁止提前实现**：摄像音频整合、录像状态联动、付费墙、模块 5。

### 4F：摄像提词单音频源集成

**输入**：模块 3 Capture/Audio 生命周期、4D 识别适配器、4E UI 语义。

**输出**：同一 Capture Session 麦克风输入的瞬时 PCM 分发；录制中语音跟随；识别故障与录像隔离；路由和中断归属。

**允许文件**：`TakeFlow/CameraRecording/**`、`TakeFlow/SpeechTracking/**`、必要的 `TakeFlow/Core/Models/**` 协议值、对应测试和文档。不得创建平行 Capture Session 或第二套录制状态。

**单元测试**：单一音频源、订阅/退订、录制中识别失败、录像中断优先级、前后台、连续录制、镜头切换、旧 session/generation、文件与恢复片段全回归。

**UI/真机**：D-10、D-11；内置和 Bluetooth 路由；语音失败后视频仍可完成播放且有声。

**完成定义**：录像文件和模块 3 状态机不受识别故障污染；模块 2 性能和模块 3 全量测试继续通过。

**发布阻断条件**：必须在真实设备证明同一 `AVCaptureSession` 内的 PCM 输出可与现有视频输出、AAC 音频录制输出共同工作；录制过程中不得为语音跟随重建 Capture Session 图，不得创建第二个 `AVAudioEngine` 或第二条麦克风采集链路。识别失败、取消或不可用不得停止、破坏、重命名或改变录制文件。若目标设备上无法安全共存，只能把语音跟随降级为固定速度提词；不得改用联网识别或第二音频源。缺少这组真机共存证据时，4F 不得完成或发布。

**禁止提前实现**：字幕、分段、拼接、付费墙、模块 5。

### 4G：性能、恢复、隐私和无障碍加固

**输入**：4E/4F 完整用户路径和冻结验收矩阵。

**输出**：长稿性能、30 分钟任务轮换、内存压力、权限/中断恢复、最大字体、VoiceOver、iPhone/iPad、Release 隔离和隐私审计闭环。

**允许文件**：仅为未通过验收而需要的 `SpeechTracking`、`Teleprompter`、`CameraRecording`、`Core/Permissions`、测试与文档；工程配置仅限已批准的 Speech 用途说明修正。

**单元/UI**：全量回归；10 万字符硬性能连续 3 轮；最大字体和 VoiceOver；错误矩阵；Release 字符串、网络和日志扫描。

**真机**：D-01 至 D-14 的剩余项目，至少一台 iPhone 和一台 iPad；记录 Bluetooth、飞行模式、30 分钟资源数据。

**完成定义**：所有可自动化项目通过，真机项目有逐项证据；任何失败保持模块进行中。

**禁止提前实现**：StoreKit、付费墙、云端识别、模块 5。

### 4H：模块 4 发布基线与交接

**输入**：4B–4G 的代码、测试、真机证据和风险记录。

**输出**：最终接口文档、状态与手动清单、隐私/权限一致性报告、模块 4 本地 Git 基线候选；不自动开始模块 5。

**允许文件**：模块 4 已有范围内的必要回归修复、测试和文档；不得扩展产品功能。

**单元/UI/构建**：全量测试 0 失败/0 跳过；iPhone/iPad Debug、Generic Device Debug/Release、警告即错误；模块 2 性能三轮和模块 3 录制回归。

**真机**：冻结矩阵全部达到数值阈值；不能稳定触发的系统事件必须用自动化证据和残余风险明确标记，不能伪造通过。

**完成定义**：隐私决定、功能、自动化和要求真机证据齐全，且产品负责人审核后，模块 4 才可标记已完成并单独提交。

**禁止提前实现**：模块 5、字幕、AI、StoreKit 或云端服务。

## 14. 尚未解决的产品决定

以下项目不阻断 4B–4G 的本地语音跟随实现，但在相关范围开始前必须解决：

1. 免费体验的计量单位、额度、重置周期、离线宽限和 Pro 权益；继续由 `SPEC-006` 阻断付费墙。
2. `zh-CN` 之外的语言、方言和用户可选 locale；首版不承诺。
3. 本地等价词表的首发词条、审核人和版本更新流程；没有批准的词条不得宣称支持任意同义改写。
4. 最低 iOS 17 设备和真实 iPad 上 `zh-CN` 本地模型的覆盖率；必须运行时探测并由设备矩阵补证。
5. 语音跟随产生的句子时间信息是否成为模块 6 字幕输入；当前不得持久化识别文本或把跟随位置冒充字幕时间线，继续由 `SPEC-005` 决定。

## 15. 发布阻断与风险

- 任一代码路径可能在 `requiresOnDeviceRecognition` 未生效或本地能力不支持时发起联网识别，阻断发布。
- 识别文本、候选、时间线、评分、临时锚点、识别音频副本或稿件内容进入 SwiftData、UserDefaults、文件、日志、分析或网络，阻断发布。
- 4F 缺少真实设备“同一 Capture Session 的 PCM、视频输出和 AAC 音频输出安全共存”证据，或摄像提词使用第二个音频采集会话、录制中重建 Capture Session 图、识别错误停止正常录像、造成无声文件或破坏模块 3 中断恢复，阻断模块 4 完成。
- 匹配器存在可复现的错误向后跳转、低置信远跳或手动锚点被旧结果覆盖，阻断模块 4 完成。
- 10 万字符路径在主线程全文扫描、缓存无界或超过硬性能阈值，阻断对应阶段完成。
- M3-13B 暂缓且未验收；模块 4 的 VoiceOver 最终矩阵必须独立执行，不能引用 M3-13A 或模拟器结果冒充通过。
