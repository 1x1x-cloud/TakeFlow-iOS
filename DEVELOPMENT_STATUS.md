# 开发状态

最后更新：2026-08-15
产品规格：`PRODUCT_SPEC.md` 1.0
总体状态：进行中
当前阶段：模块 0 已完成；模块 1、模块 2、模块 3 的实现已进入持续验收；模块 3 的 M3-01 至 M3-06、M3-12、M3-13A、M3-14 至 M3-18 已通过验收。M3-13A 在 iPhone 16 最大辅助功能字体下经历首次真机失败、核心布局修复后 3/3 通过但发现删除与音频布局残留问题、第二轮软件修复，最终完整布局矩阵、删除专项及音频设备布局专项均连续 3/3 真机通过；M3-13 整体仍因 M3-13B VoiceOver 完整矩阵未执行而保持进行中。M3-12 采用真实 iPhone 的保存成功、权限拒绝/恢复、系统分享与 VoiceOver 证据，加注入式非权限照片写入失败自动化证据；未在真机人为制造 Photos 写入失败。M3-07A 恢复片段用户闭环已由产品负责人确认真机通过；M3-07B 的锁屏 15 秒、主动进入后台 15 秒、两组微信视频、普通来电快速拒接及普通来电接听子项现均连续 3/3 通过。快速拒接属于系统未产生可观察中断的连续录制正常路径；接听约 15 秒则属于安全停止、单一恢复片段和手动重新准备的真实中断路径。此前快速拒接及其他中断场景的失败和修复记录继续保留。FaceTime 明确为本阶段未纳入验收范围，未标记通过；外部占用、系统压力和媒体服务场景仍待真实设备验证，因此 M3-07 及模块 3 整体保持进行中；模块 4 未开始

## 状态定义

- **未开始**：尚未实现该模块。
- **进行中**：已有实现，但尚未满足全部验收条件。
- **受阻**：继续实现需要产品决策、外部服务、账号或设备条件。
- **已完成**：全部验收条件有证据，构建、自动化测试和要求的真机验证均通过。

## 模块状态

| 模块 | 名称 | 状态 | 构建 | 自动化测试 | 真机验证 | 备注 |
|---|---|---|---|---|---|---|
| 0 | 工程初始化与规则建立 | 已完成 | iPhone、iPad 模拟器构建通过；正式签名的连接 iPhone Debug 与 Generic iOS Device Release 构建通过 | 5 通过、0 失败 | M3-16 已证明正式签名 App 可在 iPhone 运行；干净安装、iPad 与 TestFlight 仍未完整验证 | `TakeFlow` App、单测、UI 测试三个 Target 已建立；正式身份已配置，发布前手动项仍见 M0-01 至 M0-06 |
| 1 | 稿件管理与编辑 | 进行中 | iPhone、iPad 模拟器 Debug 构建通过 | 单元测试 33/33；UI 测试 4/4 | M1-01 至 M1-07 待执行 | SwiftData 本地持久化、V1 Schema 与独立恢复草稿已落地；状态保留为“进行中”，直到真机强杀恢复、输入、性能、离线和低存储验收完成 |
| 2 | 基础提词器 | 进行中 | iPhone、iPad 模拟器 Debug 构建通过 | 单元测试 74/74；UI 测试 9/9（含模块 1 全量回归） | M2-01 至 M2-10 待执行 | 固定速度、唯一状态机、全局 Character 锚点、每稿偏好及有界分块虚拟化已实现；模拟器 1 万/10 万字符内部阈值通过，真机滚动、辅助功能及长稿性能未验收 |
| 3 | 摄像头与视频录制 | 进行中 | M3-13A 残留修复后 iPhone、iPad 模拟器 Debug 及 Generic iOS Device Release 无签名构建通过；既有 Generic Debug 也已通过 | 最终全量单元 269/269、UI 32/32，失败 0、跳过 0；最大辅助字体覆盖稿件删除取消/确认/失败、普通提词器状态与旋转、摄像提词音频设备入口、预览释放及单/双恢复卡片 | M3-01 至 M3-06、M3-12、M3-13A、M3-14 至 M3-18 通过；M3-07A 通过；M3-07B 多项通过但其余中断矩阵待执行；M3-13B VoiceOver 完整矩阵及 M3-08 至 M3-11 等项目待执行 | 最大字体修复未改变录制状态机、AVFoundation、音频路由行为、恢复文件或编码配置；FaceTime 本阶段未纳入且未标记通过；外部占用、系统压力和媒体服务仍待真机，不得外推为 M3-07、M3-13 或模块 3 全部完成 |
| 4 | 实时语音跟随 | 未开始 | 未运行 | 未运行 | 未执行 | 语音识别隐私路径需先确认 |
| 5 | 分段录制与错句重拍 | 未开始 | 未运行 | 未运行 | 未执行 | — |
| 6 | 视频拼接、字幕与导出 | 未开始 | 未运行 | 未运行 | 未执行 | 字幕时间来源与量化标准需先确认 |
| 7 | AI 口语化改稿 | 未开始 | 未运行 | 未运行 | 未执行 | 供应商、后端与数据处理规则未定 |
| 8 | 订阅与权限控制 | 未开始 | 未运行 | 未运行 | 未执行 | 商品、权益细则与免费额度未定 |
| 9 | 隐私、稳定性与上架准备 | 未开始 | 未运行 | 未运行 | 未执行 | 依赖前序模块与外部政策/条款地址 |

## 构建与测试事实

验证日期：2026-07-31

### 模块 0 验证事实

- 工具链：完整 Xcode 26.0.1（Build 17A400），Apple Swift 6.2，iOS/iOS Simulator SDK 26.0。
- 可用运行时：iOS Simulator 26.0.1；已使用 iPhone 17 Pro 与 iPad Pro 11-inch (M4)。
- 工程解析：`plutil -lint TakeFlow.xcodeproj/project.pbxproj` 通过；`xcodebuild -list` 识别 `TakeFlow`、`TakeFlowTests`、`TakeFlowUITests` 和共享 Scheme `TakeFlow`。
- iPhone 构建：Debug、iPhone 17 Pro 模拟器，提交前基线复验 `BUILD SUCCEEDED`；产物位于被忽略的 `.build/BaselineDerivedData`。
- iPad 构建：Debug、iPad Pro 11-inch (M4) 模拟器，`BUILD SUCCEEDED`。
- 模块 0 提交前全量测试：当时共享 Scheme 共 5 项，5 通过、0 失败、0 跳过；结果包位于被忽略的 `.build/BaselineAllTests.xcresult`。
- 单元测试：`TakeFlowTests` 共 4 项，4 通过、0 失败。
- UI 测试：`testLaunchShowsMinimalHome` 共 1 项，1 通过、0 失败；测试真实启动 App 并断言 `home.title` 与 `home.status` 存在。
- 首页视觉核对：已在 iPhone 17 Pro 模拟器重新安装并启动 App，确认显示“首页”“一遍成”“工程基础已就绪”。
- 编译器未报告 Swift 源码警告。Xcode 26 的 `appintentsmetadataprocessor` 对未链接 AppIntents 的 Target 输出一次系统工具提示：“Metadata extraction skipped. No AppIntents.framework dependency found.” 本项目未使用 AppIntents，不影响产物；该提示不来自项目源码。
- 2026-07-29 已批准并配置正式 Bundle ID `com.yudiemin.takeflow`、Development Team `YW3253598N`、显示名、分类、权限用途、方向和启动配置；Debug 与 Release 的对应构建设置一致。
- 使用现有本地 Apple Development 签名资料对连接的真实 iPhone 执行 Debug build 成功，并对 Generic iOS Device 执行 Release build 成功；最低目标为 iOS 17.0。证书、私钥、描述文件和账号凭据均未加入仓库。M3-16 已证明正式签名 App 可在 iPhone 16 / iOS 26.5.2 运行，但干净安装启动、iPad 和 TestFlight 分发仍未完整验证，不能作为发布就绪证据。
- 未引入第三方包或 SDK；当前业务实现仅限模块 1、模块 2 和模块 3，模块 4 至模块 9 的目录仍只保留边界说明。

### 模块 1 验证事实

- 基线：开始实现前 `HEAD` 为 `57e15631c1f772f9e9351ea2cc1d40d4f9b7b164`，分支 `master`；模块 1 已单独提交为 `a9c13d3b3b23554f1b0578116127d3f6eebd24ff`（`feat: implement resilient script management`）。
- iPhone 构建：`xcodebuild -project TakeFlow.xcodeproj -scheme TakeFlow -configuration Debug -destination 'platform=iOS Simulator,id=4563833B-0C75-4B44-98B9-FE802962D730' -derivedDataPath .build/RecoveryDraftFinal-iPhone build`，iPhone 17 Pro / iOS 26.0.1，`BUILD SUCCEEDED`。
- iPad 构建：`xcodebuild -project TakeFlow.xcodeproj -scheme TakeFlow -configuration Debug -destination 'platform=iOS Simulator,id=08CE106F-C7F5-4CFC-8051-F216CBFDC2F1' -derivedDataPath .build/RecoveryDraftFinal-iPad build`，iPad Pro 11-inch (M4) / iOS 26.0.1，`BUILD SUCCEEDED`。
- 全量单元测试：`xcodebuild ... -derivedDataPath .build/RecoveryDraftFinal -only-testing:TakeFlowTests test`，33 项通过、0 失败、0 跳过；原模块 1 的 20 项全部继续通过，新增恢复草稿测试 13 项全部通过。结果包为 `.build/RecoveryDraftFinal/Logs/Test/Test-TakeFlow-2026.07.28_19-06-30-+0800.xcresult`。
- 10 万字符自动化基线：在上述 iPhone 17 Pro 模拟器使用隔离 SwiftData 内存容器完成正式保存、读取、字符统计和正文搜索，测试耗时 0.308 秒；独立文件恢复草稿的原子写入与读取用例耗时 0.024 秒。两者均低于当前内部保护阈值 5 秒。该数据是本轮模拟器观测值，不是最大时延保证；规格尚未批准量化性能阈值，真实 iOS 17 设备上的编辑响应仍待 M1-02 验证。
- 全量 UI 测试：`-only-testing:TakeFlowUITests`，4 项通过、0 失败；覆盖空稿件页启动、创建与编辑自动保存、复制/删除确认/短时撤销，以及使用磁盘 SwiftData Store 保存后终止并重启 App 的恢复。
- 持久化采用本地 SwiftData Store，显式禁用 CloudKit；生产路径不使用内存演示数据。UI 测试只有需要隔离的用例通过启动参数选择内存 Store，重启恢复用例明确使用磁盘 Store。
- 未完成编辑另写入 `Application Support/TakeFlow/RecoveryDrafts` 下按稳定 Script UUID 命名的独立 JSON 文件，不使用 UserDefaults 或 Caches。每份文件包含标题、正文、阅读位置、草稿时间、正式记录版本依据、编辑会话 ID 与单调 revision；写入在 actor 上串行执行，使用原子替换和文件保护，不在主线程执行大文本磁盘 I/O。
- 编辑器先显示正式 SwiftData 记录，只在恢复草稿的基础版本与正式记录精确匹配且草稿更新时间较新时提出“恢复草稿”或“保留已保存版本”，不会静默覆盖。损坏、不可读或版本不匹配均保留正式记录并显示安全错误；正式保存成功后通过提交版本屏障清理恢复文件，晚到的旧写任务不能重新覆盖或复活草稿。
- 删除稿件会同步阻止晚到草稿写入并清理恢复文件；清理失败时回滚软删除。撤销删除后重新允许该 Script ID 的恢复写入，既有复制、搜索和 5 秒撤销语义不变。
- 恢复草稿不采用额外防抖：每次编辑状态变化都立即投递不可变快照，正式 SwiftData 保存仍保持 600 ms 防抖。因此恢复路径没有人为设置的 600 ms 等待窗口，但异步任务调度、文件系统负载和进程被立即杀死之间仍不存在可证明的固定最大时延。可保证的是“最近一次已完成原子替换的恢复快照”不会出现半写入；若在最新写入完成前强杀，最后若干输入仍可能缺失，不得表述为绝对零丢失。
- 未添加网络客户端、云同步、第三方依赖、真实密钥或正式签名配置；模块 2 至模块 9 的业务功能未提前实现。
- 编译器未报告 Swift 源码警告。Xcode 26 的未使用 AppIntents 元数据工具提示仍存在，性质与模块 0 相同。
- 真机即时强制结束及两种恢复选择、中文输入法组合态、10 万字符真实编辑响应、飞行模式、低存储写入失败、iPad 外接键盘与辅助功能仍未验证，详见 M1-01 至 M1-07。

### 模块 2 验证事实

- 基线：开始实现前 `HEAD` 为模块 1 本地基线 `a9c13d3b3b23554f1b0578116127d3f6eebd24ff`（`feat: implement resilient script management`），分支 `master`；工作区开始时干净，模块 1 与模块 2 的本地基线保持独立。
- 数据迁移：当前 SwiftData Schema 为 `ScriptSchemaV2`，从 V1 使用轻量迁移；新增字号、行距、点/秒速度、左右边距、区域宽度/垂直位置、深浅模式、水平/垂直镜像和倒计时字段。自动化已用真实 V1 磁盘 Store 打开 V2 容器并验证旧稿、旧阅读位置和安全默认值可读。
- 状态与时间：`TeleprompterPlaybackMachine` 明确区分 `idle`、`countingDown`、`running`、`paused`、`userDragging`、`finished` 和类型化 `error`；位置由单调时钟的运行段起点、实际经过秒数和逻辑点/秒推导，不按帧累加。60Hz 与 120Hz 模拟 10 秒均得到 600 点，误差在测试精度 `0.000001` 点内；后台时间不累计，返回时保持暂停。
- 阅读位置：`ScriptReadingAnchor` 使用 Swift 扩展字素簇偏移，UIKit 桥接仅把它换算为当前布局像素；字号、行距、边距、区域宽度、方向和尺寸变化后重新解析锚点。拖动中只在当前块内计算轻量近似锚点，拖动结束时用当前可见 TextKit 单元解析精确全局位置；全文 Character 数与可读性已在后台索引中预计算，滚动帧不再重复扫描正文。
- 分块呈现：`TeleprompterDocument` 在后台按段落优先、Swift Character 安全边界建立内容版本与双向索引；块目标 1,500 Character、硬上限 2,200。`UICollectionView` 只实例化可见 TextKit 单元，预取前 1、后 2 块；富文本 LRU 上限为 8，内存警告时释放非可见缓存。正文版本变化会丢弃旧索引；状态机、全局锚点及 SwiftData 事实来源保持唯一。
- 数据安全：提词器通过 `TeleprompterScriptProviding` 重新读取最新正式稿后只写阅读位置和显示偏好，不持有陈旧正文快照；若存在比正式记录新的恢复草稿，阻止进入或保存提词状态并要求先在编辑器选择版本。单元测试证明保存提词状态不改正文，模块 1 的恢复、删除撤销、复制、搜索和 600 ms 自动保存测试全部继续通过。
- iPhone 提交前复验：`xcodebuild -project TakeFlow.xcodeproj -scheme TakeFlow -configuration Debug -destination 'platform=iOS Simulator,id=4563833B-0C75-4B44-98B9-FE802962D730' -derivedDataPath .build/Module2Baseline-iPhone build -quiet`，iPhone 17 Pro / iOS 26.0.1，退出码 0。
- iPad 提交前复验：`xcodebuild -project TakeFlow.xcodeproj -scheme TakeFlow -configuration Debug -destination 'platform=iOS Simulator,id=08CE106F-C7F5-4CFC-8051-F216CBFDC2F1' -derivedDataPath .build/Module2Baseline-iPad build -quiet`，iPad Pro 11-inch (M4) / iOS 26.0.1，退出码 0。
- 提交前全量单元测试：`xcodebuild ... -derivedDataPath .build/Module2BaselineVerification -only-testing:TakeFlowTests test -quiet`，74 项通过、0 失败、0 跳过；模块 1 基线 33 项全部回归通过。新增覆盖安全分块、Emoji/组合字符、双向偏移、远端跳转、跨块滚动/拖动/状态转换、内容版本失效、有界缓存、内存压力、硬性能阈值及运行期无全文重复布局。结果包为 `.build/Module2BaselineVerification/Logs/Test/Test-TakeFlow-2026.07.28_22-44-16-+0800.xcresult`。
- 提交前全量 UI 测试：`xcodebuild ... -derivedDataPath .build/Module2BaselineVerification -only-testing:TakeFlowUITests test -quiet`，9 项通过、0 失败、0 跳过；模块 1 原 4 项全部回归通过，模块 2 的 5 项覆盖可访问控件与控制栏恢复、倒计时→运行→暂停→继续、拖动后继续、字号/速度/边距退出重入恢复、空稿不能进入运行状态。结果包为 `.build/Module2BaselineVerification/Logs/Test/Test-TakeFlow-2026.07.28_22-45-25-+0800.xcresult`。
- 全量回归曾暴露模块 1 恢复文件日期仅存毫秒、内存 `Date` 保留更高精度导致旧 revision 偶发覆盖新 revision 的缺陷；版本比较已改用持久化的正式记录位版本，并把跨会话草稿时间归一化到毫秒。目标测试连续运行 10 次全部通过，结果包为 `.build/Module2FinalTests/Logs/Test/Test-TakeFlow-2026.07.28_21-01-00-+0800.xcresult`；随后 55 项全量测试再次通过。
- 优化前证据：iPhone 17 Pro / iOS 26.0.1 模拟器测试进程中，SwiftData 读取 13.353 ms、Character/段落索引 134.181 ms、富文本构造 0.321 ms、TextKit 2 整篇赋值 16,182.854 ms、首次布局 9.048 ms、首屏计算 0.657 ms、中点锚点 13.003 ms、全文高度 0.005 ms、滚动与结束锚点 80.504 ms；TextKit 1 对照整篇赋值也为 14,218.354 ms，证明主要瓶颈是整篇文本赋值而非 SwiftData、SwiftUI 或 TextKit 版本。
- 优化后分阶段观测（三轮范围）：SwiftData 读取 3.801–4.168 ms、后台块索引 91.065–106.028 ms、可见块富文本构造 0.079–0.100 ms、可见块赋值 1.061–1.335 ms、首次块布局 90.780–108.954 ms、首屏可见计算 0.176–0.186 ms、块内锚点 0.754–0.783 ms、虚拟高度与首屏 279.832–344.072 ms、桥接等价更新 0.301–0.399 ms、601 次可见区更新 41.684–50.203 ms。
- 提交前连续三轮硬性能测试均通过：1 万字符从 SwiftData 读取到首屏可交互总耗时 400.907 / 351.355 / 354.299 ms（阈值 500 ms）；10 万字符 427.399 / 329.148 / 482.963 ms（阈值 2,000 ms）。设备为 iPhone 17 Pro 模拟器、iOS 26.0.1、Debug、Xcode 26.0.1；测量包含真实内存 SwiftData 值读取、后台索引及虚拟化首屏出现可见单元，属于本机内部防退化数据，不是真机性能保证。三轮各 7 项性能测试均通过，结果包为 `.build/Module2BaselinePerfRound1.xcresult` 至 `.build/Module2BaselinePerfRound3.xcresult`。
- 字号、行距、边距和宽度的 10 万字符重排硬断言低于 1 秒；1,200 次运行更新硬断言低于 1 秒，源码路径和测试均证明不再周期性赋值或布局全文。真机连续滚动、触控、旋转、内存和热状态仍待 M2-08、M2-10。
- 模块 2 基线提交时编译器未报告新增 Swift 源码警告；当时未加入网络、权限、摄像头、麦克风、语音、媒体、StoreKit、CloudKit、第三方依赖、密钥或正式签名资料。模块 3 后续实现没有改变模块 2 的状态机、全局 Character 锚点或有界虚拟化事实来源。

### 模块 3 验证事实

- 基线：开始实现前及发布静态复验开始时，`HEAD` 均为模块 2 本地基线 `cadf791061fc5d738550249f253dba8206316e00`，分支 `master`；模块 0、1、2 提交依次为 `57e1563`、`a9c13d3`、`cadf791`。本节随模块 3 软件与模拟器本地基线提交建立；模块 4 未开始。
- 架构：`CameraRecordingViewModel` 位于 `@MainActor`，只组合权限、录制、文件、空间、照片和音频会话协议；`AVFoundationCaptureService` 在独立串行队列管理 `AVCaptureSession`、设备和 `AVCaptureMovieFileOutput`。Delegate 与系统通知转换为带录制 UUID 的异步事件，显式状态机区分 `idle`、`requestingPermissions`、`configuring`、`ready`、`starting`、`recording`、`stopping`、`finished`、`interrupted` 和 `failed`，并拒绝重复开始、重复停止、录制中切换及过期回调。
- 能力与媒体配置：运行时检查 session preset、设备 format 的 30 fps 范围及 Movie Output 可用 codec；默认 1080p/30 fps/H.264，仅在当前设备完整支持时提供 4K/30 fps/HEVC，并有类型化安全降级。音频目标为 AAC、48 kHz，系统音频会话允许可用 Bluetooth HFP 输入并将路由变化转换为明确 UI 状态；麦克风未授权或无输入时不进入正常含声录制流程。
- 方向与镜像：预览与输出 connection 独立配置，使用 `AVCaptureDevice.RotationCoordinator` 的 `videoRotationAngleForHorizonLevelPreview` 和 `videoRotationAngleForHorizonLevelCapture`；开始录制时冻结输出角度，录制中旋转只更新预览及锁定提示。前摄预览镜像、Movie Output 不镜像，后摄两者均不镜像；未使用已废弃 `videoOrientation`。
- 文件安全：录制位于 `Application Support/TakeFlow/Recordings/<project UUID>`，每次使用独立 recording UUID 和 `Temporary` 临时文件。项目元数据先原子写入，只有 Movie Output 完成回调成功后才移动到 `Segments` 并标记可播放；中断、写入失败和遗留临时文件保留为待检查/可恢复，不冒充正常文件。启动扫描、损坏元数据隔离、唯一命名、精确项目目录删除和跨实例恢复均有测试；日志不记录正文、媒体内容或完整本地路径。
- 存储空间：`RecordingStoragePolicy` 集中定义起录安全线 500 MB、录制中安全停止线 250 MB 和 5 秒轮询间隔；生产实现读取当前卷重要用途可用容量，测试通过注入服务覆盖边界。低空间、文件写入失败、中断、后台及停止竞争只提交一次安全停止，已经落盘的临时内容保留。
- 权限与导出：摄像头和麦克风按进入摄像提词流程请求；录制到私有目录不请求照片权限。用户主动保存时仅请求 Photos `addOnly`；拒绝或失败仍保留 App 内文件并允许系统分享。`PrivacyInfo.xcprivacy` 已加入 Target，磁盘可用空间 required-reason API 声明与当前实现一致。
- 提词器复用：摄像头界面直接复用 `TeleprompterPlaybackMachine`、`TeleprompterDocument`、`TeleprompterViewModel` 和现有分块 Collection View，不存在第二套滚动状态，也没有恢复整篇 TextKit 赋值。摄像头预览、录制连接与 SwiftUI 提词/控制叠加层分离，因此 UI 不进入 Movie Output 媒体流；真实录制文件像素仍需 M3-04 真机验证。
- 模块 2 性能回归期间发现可见块仍触发通用自适应布局和重复锚点校准。修复后，每个可见块使用有界 TextKit 1 单元与 `boundingRect` 高度计算，先按全局 Character 跳转目标块，再在下一次主 actor 调度中完成块内精确校准；revision 屏障避免旧布局覆盖新布局。全文索引、唯一状态机、全局 Character 锚点、块目标 1,500/硬上限 2,200 和富文本 LRU 上限 8 均保持不变。
- 最终 iPhone 构建：`xcodebuild -project TakeFlow.xcodeproj -scheme TakeFlow -configuration Debug -destination 'platform=iOS Simulator,name=iPhone 17 Pro' -derivedDataPath .build/Module3FinalBuild-iPhone build -quiet`，iPhone 17 Pro / iOS 26.0.1，退出码 0。
- 最终 iPad 构建：`xcodebuild -project TakeFlow.xcodeproj -scheme TakeFlow -configuration Debug -destination 'platform=iOS Simulator,name=iPad Pro 11-inch (M4)' -derivedDataPath .build/Module3FinalBuild-iPad build -quiet`，iPad Pro 11-inch (M4) / iOS 26.0.1，退出码 0。
- Generic iOS Device Debug 编译：`xcodebuild -project TakeFlow.xcodeproj -scheme TakeFlow -configuration Debug -destination 'generic/platform=iOS' -derivedDataPath .build/Module3DeviceDebug CODE_SIGNING_ALLOWED=NO build -quiet`，退出码 0。Release 编译使用相同 Generic iOS Device、`.build/Module3DeviceRelease`、`CODE_SIGNING_ALLOWED=NO`，并设置 `SWIFT_TREAT_WARNINGS_AS_ERRORS=YES`、`GCC_TREAT_WARNINGS_AS_ERRORS=YES`，`BUILD SUCCEEDED`；实际 arm64 产物最低版本为 iOS 17.0。首次把条件编译跨越 SwiftUI `else if` 分支时编译失败，修正为独立 `@ViewBuilder` 边界后最终复验通过，失败期间没有提交。
- Release 产品审计：模块 3 软件基线建立时，生产 App 只包含二进制、`Info.plist`、`PrivacyInfo.xcprivacy` 和 `PkgInfo`，无测试媒体、测试 Bundle、调试菜单或代码签名目录；当时仍使用占位 Bundle ID 且未写入 `DEVELOPMENT_TEAM`。UI 测试启动参数、Fake Capture 服务、Fake 权限/空间/照片/音频服务及 Fake 预览文案均由 `#if DEBUG` 隔离；对 Release 二进制执行字符串和符号扫描，相关匹配为 0。2026-07-29 后续正式身份配置没有改变该 Debug/Release 代码隔离。
- iOS 17 与权限审计：Generic arm64 iOS 17 编译在警告即错误模式下通过；源码没有无保护的 iOS 18、iOS 26 或 Beta API，也未使用已废弃 `videoOrientation`。`AVCaptureDevice.RotationCoordinator` 与 `videoRotationAngle` 在本机 iOS SDK 头文件中标注 iOS 17 可用。Release `Info.plist` 的摄像头说明为“用于在您主动进入摄像提词并开始录制时拍摄视频。”，麦克风说明为“用于在您主动开始视频录制时同步录制声音。”，照片添加说明为“仅在您主动选择保存到照片时添加已完成的视频。”；不存在 `NSPhotoLibraryUsageDescription`，代码只使用 Photos `.addOnly`。
- 隐私清单：源码与 Release App 内嵌 `PrivacyInfo.xcprivacy` 均通过 `plutil -lint`。清单声明不跟踪、无跟踪域、无收集数据类型，仅为实际使用的磁盘可用空间 API 声明 `NSPrivacyAccessedAPICategoryDiskSpace` / `E174.1`；与当前无网络客户端、无分析 SDK、无照片读取权限的代码一致。
- 最终全量单元测试：`xcodebuild ... -derivedDataPath .build/Module3ReleaseBaseline -resultBundlePath .build/Module3ReleaseBaseline-Units-Retry-20260729.xcresult -only-testing:TakeFlowTests test -quiet`，iPhone 17 Pro / iOS 26.0.1，131 项通过、0 失败、0 跳过。包含模块 1、2 的 74 项基线、56 项模块 3 状态/配置/文件/ViewModel/集成测试和 1 项可见块裁切回归测试。
- 最终全量 UI 测试：`xcodebuild ... -derivedDataPath .build/Module3ReleaseBaseline -resultBundlePath .build/Module3ReleaseBaseline-UI-Final-20260729.xcresult -only-testing:TakeFlowUITests test -quiet`，17 项通过、0 失败、0 跳过。新增 8 项 Fake Capture 流程覆盖允许→就绪→倒计时→录制→停止→本地预览、摄像头拒绝、麦克风拒绝、中断保留、低空间、录制中切换禁用、回前台不自动恢复，以及提词开始/暂停/拖动继续；Fake 仅存在于 Debug。
- 本轮 UI 首次全量复验为 16/17：失败结果包证明 App 已出现新的“正在滚动”元素，但 XCTest 仍轮询倒计时阶段的旧 SwiftUI 可访问性元素。等待器改为每次轮询重新读取可访问性树；期间模拟器服务也出现元素已经存在但 `waitForExistence` 超时及冷启动等待系统 App 约 110 秒的异常。只重启模拟器、不抹除数据后，目标流程 1/1 和最终全量 17/17 通过；没有延长原状态时限、删除测试或接受错误状态。
- 模块 2 硬性能测试使用 iPhone 17 Pro / iOS 26.0.1 模拟器、Debug、从 SwiftData 值读取和后台索引到虚拟化首屏可交互的既定测量方式。一次受模拟器服务异常迟滞影响的尝试真实失败：1 万字符 553.339 ms（读取 5.203、索引 62.222、渲染 485.864 ms），超过 500 ms；同一轮测试进程总耗时异常膨胀到 124 秒。未放宽阈值；冷重启后从零连续三轮各 8/8 通过：1 万字符 236.269 / 430.771 / 248.552 ms（阈值 500 ms），10 万字符 470.239 / 739.123 / 534.147 ms（阈值 2,000 ms）。结果包为 `.build/Module3ReleaseBaseline-Perf-Clean1-20260729.xcresult` 至 `Clean3`；远端跳转、跨块重排、运行事件和缓存上限断言均通过。这些是本机模拟器防退化数据，不是真机保证。
- 最终源码与产品静态检查通过：Swift/GCC 警告即错误构建没有项目源码警告；Xcode 的 AppIntents 元数据工具仍输出既有“未链接 AppIntents，跳过提取”提示。`plutil -lint` 对工程和隐私清单通过，`git diff --check` 通过；模块 3 软件基线扫描未发现密钥、账号、证书、签名资料、构建产物、第三方 SDK、完整照片读取权限或模块 4 代码。后续正式身份提交仅加入获批的 Team ID 与 Bundle ID，不包含签名资产或账号凭据。
- 真机重复进入缺陷根因（2026-07-29）：依赖容器会跨页面复用同一个 `AVFoundationCaptureService`，而原实现只暴露一个非广播 `AsyncStream` continuation。首次页面退出时没有取消旧 `eventTask`、使尚未完成的 `prepare()` 失效或等待 `stopRunning()` 完成，因此旧页面仍可能竞争消费第二次启动的 `.sessionReady`；旧 ViewModel 因已不可见而丢弃该事件，新 ViewModel 则永久停在 `configuring`。这与真机“绿色摄像头指示已出现但新页面黑屏并持续准备”的现象一致。
- 重复进入生命周期加固：每次页面进入建立独立 session UUID 与 ViewModel generation，Capture 事件按生命周期单独投递；退出先使代次失效、取消准备/倒计时/监控任务，再等待同一 AVFoundation 串行队列完成安全停止。配置、`startRunning()`、`stopRunning()`、输入输出修改仍全部在原有单一串行上下文；start、stop、退出均幂等，旧 session 事件不能污染新页面，未创建并行 `AVCaptureSession`。准备超过 12 秒进入类型化错误并显示“重新尝试”；重试会完整结束旧生命周期并生成新 UUID 重新配置，不是只改 UI 状态。
- 录制中退出加固：若页面退出时仍在录制，先请求一次安全停止并等待带原录制 UUID 的完成回调提交文件；8 秒安全界限后仍未完成则保留为可恢复状态，随后再停止会话。停止竞争由 `stoppingRecordingID` 去重，避免用户停止、退出和中断重复调用 Movie Output；迟到完成只允许结算精确匹配的待处理录制，不得更新已退出页面或下一生命周期。
- 重复进入修复后的最终模拟器构建：iPhone 17 Pro / iOS 26.0.1 Debug 与 iPad Pro 11-inch (M4) / iOS 26.0.1 Debug 均 `BUILD SUCCEEDED`。没有新增 Swift 源码警告；仅保留 Xcode AppIntents 元数据工具的既有“未链接 AppIntents”提示。
- 重复进入修复后的设备 SDK Release 复验：`xcodebuild -project TakeFlow.xcodeproj -scheme TakeFlow -configuration Release -destination 'generic/platform=iOS' -derivedDataPath .build/Module3RepeatedEntry-DeviceRelease CODE_SIGNING_ALLOWED=NO build`，arm64 / iOS 17.0，`BUILD SUCCEEDED`。Release App 仅含可执行文件、`Info.plist`、`PrivacyInfo.xcprivacy` 和 `PkgInfo`；字符串扫描未发现 Fake Capture、测试开关或测试摄像头文案，Debug/Release 隔离保持不变。
- 重复进入修复后的最终单元测试：`xcodebuild -project TakeFlow.xcodeproj -scheme TakeFlow -configuration Debug -destination 'platform=iOS Simulator,name=iPhone 17 Pro' -derivedDataPath .build/DerivedData -resultBundlePath .build/Module3RepeatedEntry-Units-Final-20260729-Retry.xcresult -only-testing:TakeFlowTests test`，iPhone 17 Pro / iOS 26.0.1，139 项通过、0 失败、0 跳过。新增覆盖首次进入→退出→第二次成功、连续 20 次进入退出、准备中退出后重入、重复 start/stop 幂等、首次生命周期迟到回调隔离、准备超时及真实重试、后台/中断后重入，以及录制中退出完成后再释放生命周期。
- 重复进入修复后的最终 UI 测试：iPhone 17 Pro / iOS 26.0.1，19 项通过、0 失败、0 跳过，结果包 `.build/DerivedData/Logs/Test/Test-TakeFlow-2026.07.29_20-10-10-+0800.xcresult`。新增二次进入和准备超时→“重新尝试”→恢复流程。首次全量运行中，既有拖动测试使用 20 行短稿，一次滑动已到真实结尾而得到正确 `finished` 状态；测试稿扩展为 100 行以确保断言的确是中段继续，未放宽状态时限或接受错误状态，最终全量通过。
- 本轮最终全量单元测试内的模块 2 性能回归继续通过：1 万字符从读取、索引到首屏为 98.750 ms，10 万字符为 208.356 ms；普通偏好重排为 0.324 秒，LRU 上限与唯一滚动状态机测试通过。这些是 iPhone 17 Pro / iOS 26.0.1 模拟器 Debug 的单次防退化观测，不是真机性能保证。
- M3-16 真实设备验收（2026-07-29）：iPhone 16 / iOS 26.5.2，测试版本为 `HEAD` `10480d3e37800432d2944bb10383a581be90fd3c` 加本次未提交摄像头生命周期修复。连续 10 轮执行“进入摄像提词→约 1 秒出现预览→录制 5 秒→停止→播放→退出→再次进入”，10/10 通过；每轮文件均可播放且有声音，每轮退出后绿色摄像头指示均消失，全程无需强制结束 App，未再出现黑屏或永久停留在“正在准备摄像头”。这验证了当前页面退出时在同一串行队列停止会话、下一生命周期重新初始化的真实硬件路径。
- M3-01 至 M3-06 与 M3-16 至 M3-18 已通过；M3-07 的用户可见恢复入口软件修复完成但真机仍为失败／待复测。真实防抖、Bluetooth 路由、更多系统中断、媒体服务重置、低空间封口、Photos、30 分钟热量/功耗/音画同步与异常文件可播放性仍需 M3-07 至 M3-15 验证，因此模块 3 状态保持“进行中”。
- 录制完成后继续拍摄缺陷根因与修复（2026-07-29）：`RecordingStateMachine.permitsCameraSwitch()` 原先把 `.finished`、`.interrupted` 和 `.failed` 都视为可切换终态，但 `CameraRecordingViewModel.canSwitchCamera` 又额外要求 `.ready`；正常文件封口后状态停在 `.finished`，因此同页切换和下一次起录均被禁用。当前实现保留 `.finished` 作为文件提交成功的显式状态，再仅在页面仍可见、session ID 与 lifecycle generation 仍匹配且未关闭时调用 `prepareForNextRecording` 回到 `.ready`；`completedRecording` 不随该转换清理，手动预览入口继续可用。页面退出和旧完成回调仍可安全结算精确匹配的文件，但不能把当前或下一生命周期恢复为 ready。
- 摄像头切换加固：只有 `.ready` 可进入 `beginCameraSwitch()`；切换期间状态为 `.configuring`，重复点击、起录和再次切换均禁用。生产服务继续在原有单一 AVFoundation 串行队列和当前 session 上更换输入并发送新的 `.sessionReady`；成功回到 `.ready`，失败进入可理解的 `.failed` 并使用既有真实重试流程。`.starting`、`.recording`、`.stopping`、文件封口中、`.interrupted` 和 `.failed` 均不能直接切换。
- 本轮 iPhone 17 Pro / iOS 26.0.1 模拟器 Debug 全量单元测试 151/151 通过、0 失败、0 跳过；在原 139 项上新增 12 项，覆盖完成后显式恢复 ready、最近完成文件保留、安全切换、重复点击、封口期间禁用、切换失败重试、中断/失败禁用、双段文件隔离、页面退出和旧 generation。真实 `RecordingFileStore` 临时目录测试证明第二段完成后第一段文件内容仍存在且路径、项目与 recording UUID 均不重复。
- 本轮全量 UI 测试 20/20 通过、0 失败、0 跳过；新增流程完成“前摄录制→停止封口→不自动打开预览→同页切到后摄→第二次录制→主动打开最近完成预览”。iPhone 17 Pro 与 iPad Pro 11-inch (M4) 模拟器 Debug 构建均以退出码 0 通过。
- 模块 2 硬性能回归连续三轮各 8/8 通过：1 万字符从读取到首屏为 97.062 / 98.473 / 103.889 ms（阈值 500 ms），10 万字符为 196.942 / 203.634 / 235.497 ms（阈值 2,000 ms）；LRU 上限、远端跳转、偏好重排和运行期无全文重复布局断言继续通过。设备为 iPhone 17 Pro 模拟器、iOS 26.0.1、Debug；这是本机防退化观测，不是真机性能保证。
- M3-02 首轮真实设备复测失败（2026-07-30）：iPhone 16 / iOS 26.5.2，基线 `f0354031e32282a362d70bd94c327a67afd5c02a` 加当时未提交修复。连续 5 轮中第 1、3、4、5 轮通过，第 2 轮在前摄文件完成后无法切换后摄并显示“视频录制中断”，总计 4/5；未出现黑屏、永久准备、文件覆盖、无声视频或必须强制结束 App，测试者未主动锁屏、切后台、接听电话或启动其他相机 App。因此 M3-02 状态为“失败/待复测”，不能按偶发系统错误关闭。
- 本轮进一步确认的事件来源：旧实现只有 `AVCaptureSessionWasInterrupted` 路径会在已完成录制且无活动录制时显示该“视频录制中断”提示；`AVAudioSession` 中断在 `.ready` 状态不会生成这条提示。旧实现把所有 Capture Session 中断一律映射为 `.cameraUnavailable`，没有读取 `AVCaptureSessionInterruptionReasonKey`，也没有监听 `AVCaptureSessionInterruptionEnded`，因此第 2 轮历史事件的原始 AVFoundation reason 已被当时版本丢弃，无法事后诚实还原。现有代码证据未证明发生了用户描述以外的外部中断。
- 已确认并移除的 App 内部竞争条件：原生产 `switchCamera` 会在同一正在运行的 `AVCaptureSession` 中调用完整 `configureLocked`，移除视频输入、麦克风输入和 `AVCaptureMovieFileOutput` 后再全部添加；这使“第一段刚封口→前摄切后摄”同时重建视频与音频图，存在触发 Capture Session 中断的窗口。现在所有配置、起停和输入修改仍在原单一串行队列，但同生命周期切换只在一次 `beginConfiguration/commitConfiguration` 事务中替换视频输入，麦克风输入与 Movie Output 保持连接；失败时恢复原视频输入，不创建第二个并行 session。
- M3-02 当时的中断生命周期按来源跟踪 Capture Session、AVAudioSession、App 前后台和存储监控，并以 session/generation 隔离旧通知；当时仍要求所有来源结束后才进入 `.recoveryRequired`。M3-07B 真机证据随后证明生产 Capture Service 在封口后结束旧事件流时，部分 ended 可能无法抵达，因此这一“等待全部 ended”的旧条件已由统一 interruption episode 和持久 manual-reprepare 锁存取代；用户仍必须明确重新准备，系统结束通知和回前台都不能自动进入 ready。
- 仅 Debug 构建的结构化诊断现记录单调时间、状态、lifecycle generation、截短 episode/session/recording token、摄像头位置、录制/封口/重配置标志、场景阶段、映射后及原始 Capture Session reason、音频 type/options/reason/是否 suspended、文件封口、manifest 提交和 manual-reprepare 状态。日志不含稿件正文、媒体内容或完整本地路径，Release 不输出这些诊断。
- 本轮新增/强化自动化覆盖：封口完成立即切换且无错误中断、停止/恢复/切换顺序、中断结束早于开始通知的乱序配对、第二个中断迟到时撤销恢复资格、旧 Capture 与旧音频通知隔离、无录制中断结束后的明确恢复、录制中断先保留文件再恢复、切换失败后真实重试、同页前摄→后摄双段录制，以及连续 50 轮 Fake 录制/停止/切换；双段真实文件存储测试继续证明文件路径和 UUID 唯一且第一段未被覆盖。
- 本轮最终自动化（2026-07-30）：iPhone 17 Pro / iOS 26.0.1 模拟器 Debug 全量单元测试 162/162 通过；全量 UI 测试 21/21 通过。UI 覆盖同页前摄录制完成、切后摄、再次录制及主动预览，也覆盖真实中断结束后由用户点击“重新准备摄像头”的恢复路径；中断片段测试继续断言可恢复提示存在、正常成片预览不可用且摄像头切换不可用。仅观察到 Xcode AppIntents 元数据工具在未链接 AppIntents 时跳过提取的既有提示，无新增 Swift 编译警告。
- 最终构建均通过：iPhone 17 Pro 模拟器 Debug、iPad Pro 11-inch (M4) 模拟器 Debug、Generic iOS Device arm64 Debug（无签名）及 Generic iOS Device arm64 Release（无签名），部署目标均为 iOS 17.0。Release 编译未定义 `DEBUG`，Fake Capture、测试启动参数和结构化诊断代码仍由条件编译隔离。
- 模块 2 硬性能回归在 iPhone 17 Pro / iOS 26.0.1 模拟器 Debug 连续执行三轮，24/24 项通过：1 万字符从 SwiftData 读取到首屏可交互为 206.203 / 213.865 / 196.012 ms（阈值 500 ms），10 万字符为 365.085 / 369.634 / 387.556 ms（阈值 2,000 ms）；普通偏好重排三轮为 0.707 / 0.618 / 0.709 秒，均低于 1 秒；LRU 上限、远端跳转和运行期无全文重复布局断言均通过。这些是本机模拟器防退化观测，不是真机性能保证。
- M3-02 最终真实设备复测通过（2026-07-30）：iPhone 16 / iOS 26.5.2，测试基线 `f0354031e32282a362d70bd94c327a67afd5c02a` 加当前未提交修复。连续 10 轮完成“前摄录制→停止封口→不退出页面切换后摄→后摄录制”，共生成 20 段视频，全部可播放且有声音，无文件覆盖、黑屏、视频录制中断、按钮失效、永久准备状态、重复会话或卡死，全程无需强制结束 App。因此同页连续拍摄、摄像头切换和中断恢复的 M3-02 阻断项已解除；此前 4/5 失败及其根因、修复记录继续保留。
- M3-04 真实设备验收通过（2026-07-30）：iPhone 16 / iOS 26.5.2，测试版本 `3dc947b1bb676beca743d912d116753253260106`。前摄实时预览为左右镜像，最终视频不镜像且测试纸文字正常可读；后摄预览和最终视频均不镜像；水平与垂直镜像只改变提词文字；最终视频不包含提词文字或控制按钮。所有视频均可播放且有声音，全程无异常。
- M3-05 真实设备验收通过（2026-07-30）：iPhone 16 / iOS 26.5.2，测试版本 `3dc947b1bb676beca743d912d116753253260106`。竖屏、充电口朝左横屏和充电口朝右横屏的视频方向均正确；录制中旋转设备时当前文件保持起录方向并显示方向锁定提示；停止后重新录制采用当前横屏方向；无倒置、侧躺或拉伸。所有视频均可播放且有声音，全程无异常。
- M3-03 真实设备验收通过（2026-07-30）：iPhone 16 / iOS 26.5.2，测试版本 `23e9d9875ba015e4664c5c7af2f06224f49dee7f`。前摄与后摄均显示“4K · 30 fps”并录制成功；输出为 MOV、3840×2160、HEVC、30 FPS、AAC 48,000 Hz、16:9；方向和比例正确，文件均可播放且有声音，全程无异常。1080p 编码另于 2026-08-15 以受控取证方式复核：iPhone 16 / iOS 26.5.2，测试版本 `d6c42e49925218d140564685c54b2f2d2dc8aa94`，在 TakeFlow 明确选择 1080p · 30 fps，前、后摄分别录制，并从 TakeFlow 预览的系统分享面板直接保存到“文件”，未经过照片 App；两份原始 MOV 均为 1080×1920、H.264、MPEG-4 AAC 且可播放，符合“1080p 优先 H.264”配置矩阵，竖屏 1080×1920 为方向正确的 1080p 结果，本次受控复核通过。
- M3-06 真实设备验收失败（2026-07-30）：同一 iPhone 16 / iOS 26.5.2 与测试版本。点击近处物体后未正确对焦，点击远处背景后焦点未转移，点击亮暗区域后曝光无明显调整；弱光不崩溃，录制、播放和声音正常。锁定按钮虽显示状态提示，但点击对焦本身无效，且当前实现可能只改变 UI 布尔值，不能作为真实硬件锁定证据。M3-06 保持失败／待修复。
- M3-06 根因与软件修复（2026-07-30，未提交）：原 `CapturePreviewUIView` 的点击手势位于 ZStack 底层，半透明遮罩、UIKit 提词 Collection View 和控制层会使可见区域点击无法稳定送达；同时原锁定按钮只改变 ViewModel 布尔值和提示，没有调用当前 `AVCaptureDevice`，因此 UI 不能代表真实硬件状态。修复后，预览空白区域由位于控制层下方的独立点击面接收，提词覆盖区域由现有 UIKit tap recognizer 上送窗口坐标，拖动继续由提词滚动手势处理；所有坐标最终仍交给 `AVCaptureVideoPreviewLayer.captureDevicePointConverted`，由其处理 aspect fill、镜像和旋转。点击点设置与锁定/解锁拆为两个协议操作，均在原单一 AVFoundation 串行上下文执行：点击使用设备支持的单次自动对焦/曝光；锁定成功后 UI 才显示已锁定，失败保持未锁定；解锁恢复连续自动模式；切换镜头、重配、中断和页面退出均递增操作代次并重置真实/UI 锁定状态，迟到结果不能污染新生命周期。
- M3-06 核心真机复测与残留缺陷（2026-07-30）：iPhone 16 / iOS 26.5.2，基线 `86fc880ac23bd30255adf584a2573462c92e0aa8` 加当时未提交 M3-06 修复。近处/远处焦点、亮暗曝光、对焦框位置、提词覆盖区域点击、拖动与控制按钮隔离、录制中对焦曝光、真实锁定/保持/解锁均通过；连续 5 轮无黑屏、中断、卡顿或无声视频。残留失败为：锁定后切换镜头，新设备实际已回到自动状态且 `isFocusAndExposureLocked` 已重置，但通用 `noticeMessage` 仍显示旧“焦点和曝光已锁定”，直到再次点击画面才消失。因此核心硬件行为证据保留，M3-06 仍待残留反馈修复后的 5 轮复测。
- M3-06 残留反馈根因与修复（2026-07-30，未提交）：原 `resetFocusAndExposureUIState()` 只失效操作 generation、清除进行中和锁定布尔值，没有清除写在通用 `noticeMessage` 的对焦/锁定文案；对焦框、对焦任务和锁定任务又由 View 独立持有，未随镜头代次清理。现将 `focusAndExposureNoticeMessage` 与录制完成、照片保存、中断恢复和存储提示分离；切换摄像头开始前立即递增对焦操作代次，`configuring`、分辨率切换、中断恢复、页面退出、新生命周期和新 `sessionReady` 均统一清除焦点提示并发布反馈 generation。View 监听该 generation，取消旧对焦/锁定任务和对焦框；迟到的旧镜头成功结果因操作代次与生命周期校验不能写回。通用录制/中断/存储提示不会被对焦清理误删。
- M3-06 自动化与构建（2026-07-30）：iPhone 17 Pro / iOS 26.0.1 模拟器 Debug 全量单元测试 177/177、UI 测试 24/24 通过，0 失败、0 跳过。新增/强化覆盖切换开始即未锁定且旧提示消失、新镜头 ready 无旧提示、迟到锁定成功被拒绝、无需点击新焦点即可清理、分辨率/中断/退出重入清理、照片保存等通用提示保留，以及按钮图标依据、无障碍标签和值恢复为“锁定/自动调整”。iPhone、iPad 模拟器 Debug 与 Generic iOS Device 无签名 Debug/Release 构建通过，无新增 Swift 编译警告。
- M3-06 性能回归（2026-07-30）：模块 2 硬性能测试连续三轮各 8/8 通过；1 万字符从 SwiftData 读取到首屏可交互为 122.943 / 123.830 / 149.456 ms（阈值 500 ms），10 万字符为 257.136 / 302.356 / 249.356 ms（阈值 2,000 ms）。LRU 上限、远端跳转、偏好重排和运行期无全文重复布局断言继续通过；设备为 iPhone 17 Pro 模拟器 / iOS 26.0.1 / Debug，仅作为本机防退化证据，不是真机性能保证。
- M3-17 本地预览双重音轨根因与软件修复（2026-07-31，未提交）：原本地预览在 SwiftUI `body` 内直接执行 `AVPlayer(url:)`；照片保存成功、失败或权限提示更新 `noticeMessage` 后会重新计算 View 并生成无稳定所有者的新播放器，旧播放器仍可能输出音频，界面却展示新播放器的暂停状态。现由 `LocalRecordingPlayerController` 按 recording UUID 与标准化文件 URL 独占一个播放会话，同一录像的普通界面刷新只复用该会话；切换录像先暂停、移除 KVO/播放结束通知、清空当前媒体并释放旧会话，再创建新会话。播放状态直接来自 `AVPlayer.timeControlStatus` 与 `AVPlayerItem.status`；保存到照片和分享前先暂停，完成后不自动继续；工具栏关闭、下拉关闭、离开摄像页面和切换录像均统一清理，迟到的旧会话事件由 generation 拒绝。
- M3-17 单播放器真实设备验收与可见反馈缺陷（2026-07-31）：iPhone 16 / iOS 26.5.2，基线 `86fc880ac23bd30255adf584a2573462c92e0aa8` 加当前未提交 M3-06、M3-17 修复。播放中保存连续 5/5 通过：保存时播放器真实暂停，保存后不自动续播，再次播放始终只有一路声音；分享、下拉关闭和照片权限拒绝均通过，无双重音轨、后台声音、崩溃或卡死。新增失败为视频实际已保存到照片，但成功文案写入被 Sheet 遮挡的摄像主页面 `noticeMessage`，当前预览内看不到“已保存到照片”。因此单播放器真机行为保留为通过，M3-17 整体记录为“保存成功可见反馈待修复和复测”。
- M3-17 保存反馈根因与软件修复（2026-07-31，未提交）：照片保存原由摄像主页面 ViewModel 执行并把结果写入通用 `noticeMessage`，预览 Sheet 覆盖主页面后用户无法看到成功或错误。现由 `LocalRecordingPlayerController` 直接拥有 `idle`、`saving`、`saved`、`permissionDenied`、`failed` 状态；点击保存立即暂停唯一播放器，保存中禁用重复请求，当前 Sheet 显示并通过 VoiceOver 宣告“正在保存…”或结果，成功后按钮变为“已保存”且不自动关闭或续播，拒绝/失败后保留预览、分享和重试。每次保存绑定 recording UUID、标准化文件 URL 与保存 generation；关闭或切换录像会取消任务、递增 generation 并清理反馈，迟到结果不能污染下一次预览。照片状态不再写入录制、中断、存储或对焦曝光的通用提示。
- M3-17 自动化、构建与发布隔离（2026-07-31）：iPhone 17 Pro / iOS 26.0.1 模拟器 Debug 全量单元测试 187/187、UI 测试 25/25 通过。新增 10 项播放器边界测试覆盖同录像刷新、保存成功/失败/拒绝、多次保存、分享、关闭重开、录像切换、迟到事件和活动会话上限；新增 UI 流程覆盖“录制→预览→播放→保存→真实暂停状态→再次播放→关闭重开”，Debug 计数证明任意时刻活动播放器不超过 1。iPhone 与 iPad 模拟器 Debug、Generic iOS Device 无签名 Debug/Release 构建均通过；Release 二进制不含 Fake 播放器、UI 测试参数、测试媒体或 Debug 计数文案，工程身份、签名、权限与 Scheme 未由本轮修改。
- M3-17 保存反馈自动化、构建与性能（2026-07-31）：iPhone 17 Pro / iOS 26.0.1 模拟器定向播放器与照片状态测试 22/22、全量单元测试 196/196、全量 UI 测试 25/25 通过。首轮 UI 为 24/25，唯一失败是既有前后台恢复提示等待 5 秒超时；该用例定向复跑通过，第二轮全量 25/25 通过。新增 12 项照片状态测试覆盖开始保存、防重复、成功可见状态、真实暂停、拒绝、失败、权限修复后重试、关闭/切换录像后的迟到结果隔离、成功后防重复、VoiceOver 文案及状态变化不创建第二播放器；UI 流程直接在当前预览 Sheet 断言“正在保存…”和“已保存到照片”，并持续断言活动播放器上限为 1。iPhone、iPad 模拟器 Debug 与 Generic iOS Device 无签名 Debug/Release 构建通过；Release 产品不含 Fake 播放器/照片服务、UI 测试参数、测试媒体或 Debug 计数文案。模块 2 硬性能三轮各 8/8 通过：1 万字符 182.490 / 173.741 / 170.730 ms，10 万字符 377.442 / 355.189 / 420.980 ms，分别低于 500 / 2,000 ms 阈值。
- M3-17 性能回归（2026-07-31）：模块 2 硬性能连续三轮各 8/8 通过；1 万字符首屏 181.809 / 220.994 / 219.287 ms（阈值 500 ms），10 万字符首屏 385.037 / 436.096 / 513.425 ms（阈值 2,000 ms）。有界 LRU、远端跳转、偏好重排及运行期不重复全文布局继续通过；设备为 iPhone 17 Pro 模拟器 / iOS 26.0.1 / Debug，仅作为本机防退化证据。
- M3-18 真实设备失败证据与根因推断（2026-07-31）：iPhone 16 / iOS 26.5.2，基线 `86fc880ac23bd30255adf584a2573462c92e0aa8` 加未提交 M3-06、M3-17 修复。三次分别在界面 00:10、00:30、01:00 停止，最终视频均固定多约 2 秒，分别为 00:12、00:32、01:02。原实现从发出 `startRecording` 起每 250 ms 显示 `movieOutput.recordedDuration`，而停止后采用完整 `recordedDuration`；固定偏移与 2 秒 `movieFragmentInterval` 完全一致，因此强推断实时属性未计入当前未完成分片，不属于整数取整或刷新频率的正常误差。
- M3-18 软件修复与自动化（2026-07-31，未提交）：保留 2 秒媒体分片和最终媒体事实时长，不增加固定补偿。状态机新增“已请求开始、等待 AVFoundation 确认”状态；生产服务以最低 iOS 17 可用的 `fileOutput(_:didStartRecordingTo:from:)` 作为真实写入开始边界，并同时校验 recording UUID、session UUID 与 session generation。只有有效确认到达后才进入 `recording`、显示方向锁定并以 `DispatchTime.uptimeNanoseconds` 建立不受系统时间调整影响的单调起点；250 ms 更新从该起点推导，不再读取分片提交进度。重复或迟到确认不会创建第二计时器，停止、封口、中断、退出和旧生命周期均停止计时；8 秒内未收到开始确认会进入可理解的启动超时失败并清理未起录项目。最终完成模型仍只采用封口回调的有限、非负媒体时长。iPhone 17 Pro / iOS 26.0.1 模拟器 Debug 全量单元测试 212/212、UI 测试 25/25 通过；iPhone、iPad 模拟器 Debug 与 Generic iOS Device 无签名 Debug/Release 构建通过，无新增 Swift 编译警告。模块 2 硬性能连续三轮各 8/8 通过：1 万字符 223.517 / 405.189 / 234.894 ms，10 万字符 529.601 / 538.903 / 544.907 ms，分别低于 500 / 2,000 ms 阈值。该软件验证阶段仍保持失败，直到随后的真实 iPhone 10/30/60 秒及 1080p/4K 复测通过。
- M3-18 自动化过程保留（2026-07-31）：首次全量单元测试为 209/211，两项新增用例错误地等待很短的瞬时中间状态，而 Fake 已完成取消封口并进入最终安全状态。未降低产品断言；测试改为等待“仅停止一次、未进入 recording、未起录项目已清理”等最终安全结果，并在页面退出后补充显式状态复位；定向复跑通过，最终全量 212/212 通过。
- M3-18 最终真机验收通过（2026-08-01 记录）：iPhone 16 / iOS 26.5.2，后摄。1080p 三段的界面/最终时长为 00:10/00:10、00:30/00:30、01:00/01:01；4K 三段为 00:10/00:10、00:30/00:31、01:00/01:00。六段的实际差值均不超过 1 秒，无固定 2 秒偏移且误差不随时长增长；请求开始后不提前计时，AVFoundation 确认写入后才显示方向锁定和从 00:00 开始的计时。2 秒媒体分片保留，六段均可播放且有声音，无黑屏、中断、空文件、损坏文件、永久启动等待或强制结束 App。M3-18 由“失败”改为“通过”，原始失败数据、分片根因强推断与单调时钟修复记录全部保留。
- M3-06 最终真机验收通过（2026-07-31 记录）：iPhone 16 / iOS 26.5.2，基线 `86fc880ac23bd30255adf584a2573462c92e0aa8` 加未提交修复。核心对焦曝光连续 5/5、残留锁定状态连续 5/5 通过；镜头切换开始即清除旧锁定提示，新镜头按钮与 VoiceOver 恢复未锁定，无迟到任务恢复旧状态，且无黑屏、中断或强制结束 App。首轮点击无对焦、虚假锁定及随后镜头切换提示残留的失败历史均保留。
- M3-17 最终真机验收通过（2026-07-31 记录）：同一 iPhone 16 / iOS 26.5.2 与未提交 M3-06、M3-17 修复。单播放器保存连续 5/5 通过；保存反馈连续 3/3 通过。保存中按钮显示“正在保存…”并禁用，当前预览显示“已保存到照片”且按钮更新为“已保存”；每轮只新增一份视频，权限拒绝错误位于当前预览，恢复“仅添加照片”后可重试，VoiceOver 可读取各状态。最初双重音轨及成功提示被 Sheet 遮挡的失败历史均保留。
- M3-01 最终真机验收通过（2026-08-01 记录）：iPhone 16 / iOS 26.5.2，测试版本 `8dbdae330d7ccab251c36c055b8caf47bd47c5f1`。第一次“还原位置与隐私”验证首次拒绝与恢复：App 启动和普通提词器均不请求摄像头或麦克风；只有进入摄像提词才请求摄像头，拒绝后不继续请求麦克风，说明清楚且可返回稿件，再次进入不循环弹窗；在系统设置开启摄像头后才首次请求麦克风，拒绝麦克风时不开始录制、不生成被误认为正常的无声视频，再次进入不循环弹窗；在系统设置开启麦克风后预览恢复，视频可播放且有声音。第二次“还原位置与隐私”验证首次全部允许：App 启动和普通提词器仍不请求权限，进入摄像提词后依次请求摄像头和麦克风，两项允许后预览、起停录制均正常，视频可播放且有声音，退出后绿色摄像头指示消失且无重复权限弹窗。全程未请求照片读取权限，未在非摄像功能中访问相机或麦克风；无黑屏、永久准备、崩溃、卡死或强制结束 App，异常为无。
- 本轮仅更新 M3-01 真机验收文档，生产源码、测试源码、工程与 Scheme 均未改变，因此未重复运行既有 212/212 单元测试和 25/25 UI 测试；最近一次完整自动化和构建证据保持有效，但不将本次纯文档提交表述为重新执行。
- 模块 3 整体仍为“进行中”：M3-01 至 M3-06、M3-16 至 M3-18 已通过真实 iPhone 验收；M3-07 至 M3-15 的真实防抖、蓝牙音频、更多系统中断、低空间、相册、30 分钟录制、热量与音画同步，以及 DEV-05 的 iPad 方向覆盖等项目仍待执行。模块 4 仍为“未开始”。
- M3-07 用户入口阻断历史（2026-08-01）：真机测试开始后确认 `preserveRecoverableRecording` / `recoverPendingRecordings` 能把中断文件保存在 `Application Support` 私有目录，但 ViewModel 只暴露最后一项，摄像页面预览按钮又只读取 `completedRecording`。因此用户只能看到“可恢复片段”提示，不能检查播放、声音、分享、保留或删除。开发者通过 Xcode 下载 App 容器查看文件不属于用户功能，也不作为验收通过证据；M3-07 保持失败。
- M3-07 数据状态与恢复入口（2026-08-02，未提交）：manifest schema 保持向后兼容并新增 `retaining`、`retainedInterrupted`、`damaged`，明确区分正常完成片段、用户确认保留的中断片段、待检查片段和损坏片段。启动扫描返回按发现时间排序的全部恢复项；页面逐项显示中断原因、发现时间和“检查片段”。检查由独立 AVFoundation 服务异步读取文件存在性、容器、有限且大于零的媒体时长、视频轨道、音频轨道及 `isPlayable`；无音轨明确警告，损坏/零时长不开放正常播放，但仍由用户选择稍后处理或二次确认删除。每项操作带独立 operation generation，并与 session UUID、lifecycle generation 一起校验，旧扫描或迟到结果不能覆盖当前页面。
- M3-07 文件所有权与原子迁移（2026-08-02，未提交）：验证通过后“保留片段”先原子写入 `retaining` manifest，再在同一项目内把精确 recording UUID 的临时文件移动到 `Segments`，最后原子提交真实媒体时长和 `retainedInterrupted`。目标已存在、移动或最终 manifest 写入失败时不覆盖文件；启动扫描仍会从临时或正式位置重新发现处于 `retaining` 的原文件。删除必须同时匹配 project UUID 与 recording UUID，拒绝删除正常完成或已确认保留的片段。中断片段预览复用 M3-17 的唯一播放器，关闭、迁移和切换媒体前释放旧会话；保留后仍显示“中断片段，可能不完整”，照片保存继续只由用户主动触发 `.addOnly`。
- M3-07 自动化与构建（2026-08-02）：iPhone 17 Pro / iOS 26.0.1 模拟器定向恢复媒体、文件、ViewModel 和播放器 110/110 通过；补充旧 schema 1 恢复记录迁移用例后，最终全量单元测试 232/232、全量 UI 26/26 通过。UI 新增“中断→发现卡片→验证→唯一播放器检查→保留”及“删除前确认→精确删除”。删除 UI 首次 0/1 失败是 XCTest 的现代确认菜单只暴露破坏性按钮、没有可匹配的“取消”元素；测试改为断言确认菜单真实出现后点击其唯一破坏性操作，定向复跑通过，最终全量 26/26 通过，未绕过产品二次确认。iPhone/iPad 模拟器 Debug 与 Generic iOS Device 无签名 Debug/Release 均在 Swift/GCC 警告即错误下通过。首次 Generic Debug/Release 并行构建因本机磁盘只剩约 102 MB 而失败，清理仅本轮 `.build/M307*` DerivedData 后顺序重跑通过；不是源码错误。
- M3-07 模块 2 性能回归（2026-08-02）：同一 iPhone 17 Pro / iOS 26.0.1 模拟器连续三轮各 8/8 通过。1 万字符从 SwiftData 读取到首屏可交互为 265.169 / 87.789 / 86.559 ms（阈值 500 ms）；10 万字符为 746.656 / 194.279 / 203.129 ms（阈值 2,000 ms）。恢复卡片没有改变唯一滚动状态机、分块索引或 LRU 上限 8；结果仍是模拟器内部防退化数据，不替代真机指标。
- M3-07A 真机结论（本轮开始前确认）：产品负责人已确认恢复片段的用户闭环真机验收通过。原始“文件存在但用户没有检查入口”的失败历史继续保留；M3-07A 的通过不代表系统中断生命周期矩阵通过，也不将开发者下载 App 容器检查文件当作用户证据。
- M3-07B 真机失败证据（本轮开始前确认）：普通来电后立即拒接时曾显示类似“已保留可恢复片段”的提示，但没有恢复卡片，退出重进仍没有片段，也没有“重新准备摄像头”，页面无法继续录制；锁屏中断能生成片段，但检查后选择“稍后处理”会使重新准备入口消失，直至退出重进；主动进入后台后提示被延迟音频通知改写为“麦克风暂时被系统或其他应用占用”，没有片段或重新准备入口；录制中接听 FaceTime 能生成有声可播放片段，但返回后缺少重新准备入口；FaceTime/微信视频与 TakeFlow 切换时系统未必产生独立摄像头占用 reason，代码不得伪造该原因，而实际音频占用提示结束后仍可能没有恢复入口。M3-07B 因此保持失败。
- M3-07B 根因与软件修复（2026-08-02，未提交）：旧 ViewModel 允许 Capture、Audio、App 生命周期和封口回调分别改写最终 UI 状态；单个 `interruptionReason` 会被后到的低优先级音频通知覆盖，恢复资格又依赖所有来源结束，而生产 Capture Service 在封口后会结束旧事件流，导致部分 ended 永远无法抵达。另有目录扫描可能早于 manifest 原子提交，界面曾在提交完成前显示成功文案。现为每次中断建立带 episode UUID、recording/session UUID、lifecycle generation、来源与原因集合、首次时间、起录状态、封口请求/AV 完成/manifest 提交和 manual-reprepare 锁存的统一 episode；主原因固定按媒体服务、系统压力、后台、摄像头占用、音频占用、未知的优先级选择。录制或待起录中断只请求一次停止，匹配回调完成后才验证非空文件、原子提交 recoverable manifest、直接发布卡片并补扫队列；失败不显示虚假成功。`requiresManualReprepare` 与恢复卡片、Sheet 和提示完全解耦，只有用户明确重新准备且新 session/generation 完成配置、`startRunning()` 并收到 ready 后才清除。Debug 结构化诊断只记录短 UUID、代次、原始原因、场景、封口/manifest/锁存状态，不含正文、媒体或完整路径。
- M3-07B 自动化增量与当前验证状态（2026-08-02，未提交）：在保留 M3-07A 全部测试的基础上新增 15 项单元边界，并强化 2 条现有 UI 流程，覆盖快速来电 began/interrupted/ended、ended 早于 didFinish、待起录中断、manifest 延迟、后台原因优先、检查/稍后/保留/删除/Sheet 关闭不清锁存、显式重配、新旧 session 隔离、多来源单 episode、提交失败无虚假成功及无系统摄像头 reason 时不伪造占用。Swift 语法解析、`git diff --check`、工程/隐私清单格式和 Scheme XML 检查通过。当前受限执行环境无法连接 CoreSimulator，且 SwiftData 宏插件被系统沙箱拒绝；本轮全量单元/UI、四种构建和三轮性能尚未取得可引用通过结果，不能沿用 M3-07A 的 232/232、26/26 冒充本轮验证。
- M3-07B 外部 Xcode 首轮自动化与失败根因（2026-08-02，未提交）：产品负责人在 Xcode 执行真实 Product → Test，工程编译通过，共执行单元测试 251 项、UI 测试 26 项；单元 247/251、UI 25/26。4 项失败单元共报告 6 个 issue，另有 1 个 UI issue。`testFocusIsRejectedOutsideReadyAndRecordingStates` 与 `testInterruptionAndNewLifecycleDoNotRetainFocusFeedback` 仍等待旧的瞬时 `.interrupted`，后者还从已分离的通用 notice 读取中断提示；统一 episode 对无活动录制的中断会直接结算到稳定 `.recoveryRequired`，中断文案由 `interruptionNoticeMessage` 持有。`testInterruptionBeforeDidStartWaitsForAVFinishWithoutRecovery` 在关闭 Fake 自动起录后仍使用只观察 `.recording` 的 helper，没有取得 `.awaitingRecordingStart` 的请求 ID；这三项属于过期测试边界。`testRecoveryCommitFailureNeverPublishesFalseSuccess` 暴露真实生产缺陷：episode 只记录 manifest 是否成功提交，没有记录提交尝试是否已失败结束，导致失败后 `isWaitingForRecoveryCommit` 永远为真、“重新准备摄像头”一直禁用。UI 用例仍断言旧“可恢复片段”通知，而新严格顺序以“发现中断录制片段”加已提交卡片表达成功，并独立显示“重新准备摄像头”，属于过期文案断言。
- M3-07B 首轮失败修正（2026-08-02，未提交、待复跑）：`InterruptionEpisode` 现分别记录 AVFoundation 封口与 recoverable manifest 的“已完成处理”和“成功”事实；manifest 失败会结束等待、保留失败状态且不发布卡片或虚假成功。所有封口等待者改为可同时登记并在成功、失败、取消、旧生命周期失效和保护超时路径统一恢复，失败的 pending 引用从当前生命周期移除但不删除磁盘原文件。三项过期单测改为验证稳定安全状态与正确的待起录 ID；恢复提交失败测试同时验证无卡片、无“已保留”、失败可理解且显式重新准备可进入新 ready。UI 用例验证提交后“发现中断录制片段”卡片和持久“重新准备摄像头”入口，不依赖提前成功文案。Swift 语法解析、iOS 17 目标下相关生产源码 Swift 6 严格并发及警告即错误类型检查、`git diff --check`、工程文件、Scheme XML 和隐私清单格式检查通过；Generic Device 工程构建进入 Swift 编译后被当前系统沙箱拒绝启动 SwiftData 宏插件，CoreSimulator 也无法从该隔离进程连接。用户要求的失败用例连续三轮及完整 Product → Test 因此仍为未验证，M3-07 保持“失败／修复验证中”。
- M3-07B 定向测试挂起与生命周期收束修复（2026-08-03，未提交、待外部复跑）：产品负责人报告单独运行 `testInterruptionAndNewLifecycleDoNotRetainFocusFeedback` 超过 5 分钟仍停留在 `Testing TakeFlowTests`，只能手动停止；停止前最后日志为 `state=failed`、无 episode/recording/manual-reprepare/reason，按真实挂起处理。当前可读取的手动停止 xcresult 只记录“Test operation was canceled”，且其控制台明确显示实际启动的用例是 `testFocusIsRejectedOutsideReadyAndRecordingStates`，不是报告中的新生命周期用例；该证据不能被错误归到另一用例。它确认的准确挂起点是首个 `prepare()` 的 `startPreview`：测试在看到 `.configuring` 后过早发出恢复信号，但 Fake 尚未登记 checked continuation，信号被丢弃；随后 continuation 才挂起，1 秒准备保护把 UI 改为 `.failed`，但没有恢复该 continuation，所以测试方法永远不返回。Fake 现把“取消悬挂”作为锁存状态并在恢复时同时关闭悬挂开关，即使恢复早于 continuation 登记，后续 `startPreview` 也不会再挂起。独立代码审计还确认旧 Fake Capture 在 `stopPreview` 后没有结束对应 `AsyncStream`，悬挂的预览、切镜头和对焦锁定 continuation 未统一恢复；ViewModel 只取消 Capture/Audio 观察任务而不等待退出，使第二次 `prepare()` 存在与旧消费任务并存的风险。现将顺序固定为：先使 generation 失效，停止旧 Capture Session 并结束旧事件流，恢复精确归属旧会话的 continuation，取消并 `await` 排空 Capture/Audio 观察任务，停用音频，最后才允许新 UUID/generation 订阅新流并配置到真实 `sessionReady`。Debug/测试新增 recoveryRequired、退出、旧流排空、第二次 prepare 和新 ready 阶段证据，并新增“准备任务悬挂期间退出后仍可重建”边界；未增加 timeout 或固定 sleep。相关生产 ViewModel 和测试文件已分别通过 iOS 17 / Swift 6 警告即错误类型检查，但受当前隔离环境 CoreSimulator 不可用影响，两个相关用例的外部定向结果、目标连续 10 轮、其余定向三轮及完整 Product → Test 仍待 Xcode 执行；M3-07 继续保持“失败／修复验证中”。
- M3-07B 恢复检查 Sheet 下拉定向失败与修正（2026-08-04，未提交、待外部复跑）：外部 Xcode 的 `testCaptureInterruptionRetainsRecoverableSegment` 执行 57.294 秒，恢复提交、卡片、验证、播放入口、保留及中断标记均通过，唯一失败为第 562 行从警告文字执行 `swipeDown()` 后 Sheet 未消失。对应 xcresult 的失败层级显示警告 `capture.recoveryWarning` 位于覆盖整个 Sheet 的内部 `ScrollView`，frame 仅约 196×20 pt；合成事件实际从 `(201.8, 205.0)` 移到 `(201.8, 215.3)`，纵向只有约 10.3 pt，手势被文字尺寸压缩并由滚动区消费。产品 Sheet 没有 `interactiveDismissDisabled`，因此确认为 UI 测试手势定位缺陷，而非恢复状态或播放器所有权缺陷。现为检查页根容器增加稳定 `capture.recoveryReviewSheet` 可访问性标识和可见拖动指示器，测试改为从 Sheet 顶部到底部执行真实长距离下拉；关闭后同时断言 Sheet 根和警告消失、Debug Fake 活动播放器为 0、`capture.retry` 仍为“重新准备摄像头”且恢复卡片仍在。“稍后处理”显式流程继续由独立 UI 用例覆盖。Swift 语法解析和 `git diff --check` 已通过；受当前进程无法连接 CoreSimulator 影响，目标 UI 连续 5 轮、其余定向与完整 Product → Test 仍待外部 Xcode 复跑，M3-07 继续为“失败／修复验证中”。
- M3-07B 最新外部全量与剩余两项单测修正（2026-08-04，未提交、待外部复跑）：产品负责人再次执行 Xcode Product → Test，工程编译通过，UI 测试 26/26、0 失败；恢复中断 UI、Sheet 根定位、真实长距离下拉、播放器释放和持久“重新准备摄像头”入口均通过。单元测试为 251/253，2 项失败共 5 个 issue。`testPreparationTimeoutShowsRetryAndRetryRecovers` 暴露生产生命周期竞争：准备超时先发布 `.failed`，旧 session 的停止、事件流排空和音频释放随后异步完成，用户立即 retry 时可能与旧清理重叠，结果只配置一次并停在 `.configuring`。现以可等待的生命周期清理屏障串行化超时收尾、页面退出、retry 和新 prepare；新流程必须在旧清理完成后创建不同 session UUID / lifecycle generation，并由新 session 的 `sessionReady` 进入 `.ready`。`testFinalMediaDurationOverridesLiveEstimate` 属于 Fake 握手不确定：Fake 自动同步发送 `recordingStarted`，测试轮询可能错过瞬时 `.awaitingRecordingStart`；现仅在该测试关闭自动确认，取得状态机生成的真实 recording UUID 后显式发送匹配 didStart，再验证实时估算和最终媒体时长覆盖，未改变 M3-18 生产计时语义。相关 Swift 文件语法解析、`git diff --check` 和 Scheme 噪声排除已通过；当前隔离进程仍无法连接 CoreSimulator，要求的两项各连续 10 轮、既有 M3-07 定向测试、完整 253/253 与 26/26、后续构建/性能/Release 审计仍待外部 Xcode 执行，不得记为通过。
- M3-07B 最新外部全量 UI 竞争与确定性握手（2026-08-04，未提交、待外部复跑）：产品负责人读取完整 Product → Test 的 xcresult，工程编译通过，单元 253/253；UI 25/26，唯一失败为 `testCaptureInterruptionEndOffersExplicitCameraRecovery` 未在 6 秒轮询中稳定命中“正在录制”。本轮直接读取该 xcresult 活动日志和附件：点击录制后先经历 3 秒倒计时；11:29:37 的层级仍为倒计时，11:29:40 的同一轮快照已显示“正在录制”，但 Fake 从 `recordingStarted` 起固定 3 秒后自动发送中断与结束，状态在 XCTest 下一次谓词采样前又推进，故为 Debug Fake 时序竞争，不是生产状态机未起录。`.interruptionEnds` 现不再自动睡眠中断：Fake 确认起录后持续保持 `.recording`；UI 测试必须先观察“正在录制”，再点击仅在 `DEBUG`、`-ui-testing` 与 `-ui-testing-capture-interruption-ends` 同时存在且当前状态为 `.recording` 时才编译和呈现的测试入口。入口由同一 Fake actor 对当前 active recording/session 的事件流顺序发送 `interrupted → recordingFinished → interruptionEnded`，随后仍验证已提交恢复卡片、持久“重新准备摄像头”、稍后处理不清入口、用户 retry 创建新 ready 且不自动录制。正常 Debug 无入口，Release 因条件编译不含协议、入口、字符串或触发逻辑。Swift 语法解析和 `git diff --check` 已通过；当前隔离进程的 Generic Simulator 构建仍因 CoreSimulator 不可连接及 SwiftData 宏插件沙箱响应异常而失败，未将其记作源码失败或构建通过。目标 UI 连续 10 轮、完整 253/253 与 26/26、后续构建/性能/Release 产物审计仍待外部 Xcode 执行，M3-07 保持“失败／修复验证中”。
- M3-07B 软件自动化最终结论（2026-08-04，未提交）：产品负责人已在外部 Xcode 完成修正后的自动化验证。`testPreparationTimeoutShowsRetryAndRetryRecovers` 连续 10/10 通过，证明旧准备任务完成和清理后 retry 能建立新 session/generation 并进入 ready；`testFinalMediaDurationOverridesLiveEstimate` 连续 10/10 通过，证明显式 didStart 握手和最终媒体时长覆盖稳定。完整 Product → Test 为单元 253/253、UI 26/26，失败 0、跳过 0；左侧红叉确认属于历史测试状态，不是本轮失败。由生命周期清理、Fake 起录/中断确定性握手及 UI 观察竞争引起的软件自动化阻断项已解除。该结论只证明软件与模拟器边界，不能替代普通来电、锁屏、后台、FaceTime/微信切换、音频中断和媒体服务事件的真实硬件行为；M3-07 仍等待 iPhone 16 真机复测，模块 3 保持“进行中”，模块 4 保持“未开始”。
- M3-07B 锁屏与主动后台分项真机结果（2026-08-04）：iPhone 16 / iOS 26.5.2，测试版本 `660ad4bbc0425c07b1e37c2f4234a62e05d735d7` 加当前未提交 M3-07 修复。锁屏 15 秒再解锁连续 3/3 通过：每轮仅一个恢复片段，卡片自动出现，片段均可播放且有声音；选择“稍后处理”或关闭检查页后“重新准备摄像头”仍存在，重新准备成功且不自动录制、无文件覆盖。主动进入后台 15 秒再返回连续 0/3：录制均能安全停止，中断原因和重新准备入口正常，重新准备后预览恢复且不自动录制，但三轮均没有恢复片段卡片，因而无法检查播放、声音、稍后处理或保留。锁屏子项保留为通过，主动后台子项为失败，M3-07 整体继续“失败／修复中”。
- M3-07B 主动后台专属诊断边界（2026-08-04，未提交）：本轮工作区和可访问的 Xcode 结果中没有该三轮真机的导出 `capture_debug` 控制台记录；当前隔离进程也无权读取系统统一日志。因此不能据空缺日志断言真机的 `didFinish` 已到达、文件已非空或 manifest 已提交。源码审计确认两个只影响主动后台的确定缺口：`sceneDidEnterBackground()` 原先只启动普通 Swift `Task` 请求停止，没有向 iOS 申请仅用于封口的有限后台执行时间；`sceneDidBecomeActive()` 原先只发出 foreground/interruption-ended，没有重新扫描恢复队列。稳定 0/3 与“App 可能在 didFinish→文件验证→manifest 原子提交前被系统挂起，且回来后没有补扫”的路径一致，但 didFinish、文件和 manifest 的具体停点仍须由下一轮新增结构化日志确认，不把该强推断写成已读取日志的事实。
- M3-07B 主动后台软件修复（2026-08-04，未提交、待验证）：新增可注入的 `RecordingBackgroundTaskManaging` 边界；录制中进入后台并锁存 interruption episode 后，只为“停止录制→等待匹配 didFinish→验证非空文件→原子提交 recoverable manifest”申请一个有限 `UIApplication` 后台任务，绝不允许后台续录。匹配 recording/session/generation 的完成或失败路径在 manifest 结算后幂等结束后台任务；系统到期或无法取得后台任务时记录真实未提交状态、保持手动重新准备入口且不发布卡片。回到前台后按当前 session/generation 补扫队列；当前 episode 的临时文件只有 episode 已记录 manifest 提交成功才可由补扫发布，避免把尚未封口或仅被目录扫描看到的文件伪装成恢复成功。旧 session/generation 的到期或扫描结果不能污染新生命周期。
- M3-07B 后台诊断与自动化增量（2026-08-04，未提交、待完整外部复跑）：Debug 结构化记录新增后台任务 active/expired/ended、didFinish 回调、文件存在/非空、manifest started/committed/failed、前台扫描开始/完成及恢复项数量；只记录短 UUID、代次和状态，不含正文、媒体或完整路径。新增 7 项 ViewModel 边界覆盖后台 didFinish 延迟到达、didFinish 在前台后到达、manifest 提交前后台任务到期、前台补扫且单卡片、旧后台到期回调隔离、后台 manifest 失败无虚假成功，以及无卡片时重新准备入口独立保留；另新增 1 项文件 Store 测试，证明前台“仅已提交”扫描不会发布仍处于 recording/prepared 的开放孤立文件，而新生命周期/重启扫描会先原子提升 manifest 再暴露。当前源码静态统计为单元测试 261 项、UI 测试 26 项；所有本轮改动 Swift 文件均通过语法解析，`git diff --check`、工程文件、Scheme XML、隐私清单与 `.addOnly` 权限审计通过。已实际尝试 Generic iOS Device Debug 和 Release 无签名构建，两者均进入 Swift 编译后因当前隔离进程无法启动 SwiftData 宏插件而失败；`-showdestinations` 同时确认 CoreSimulator 服务不可连接，仅返回占位目标。因此本轮 261 项单元测试、26 项 UI 测试、iPhone/iPad 模拟器构建、Generic Device Debug/Release 及三轮性能均不得记为通过，须由外部 Xcode 继续验证；该环境失败不是本轮摄像代码已确认的编译错误，但也不能视为构建通过。
- M3-07B 主动后台卡片不可见的最终诊断与布局修复（2026-08-04，未提交、待真机复测）：同一个 `view_model_id=01FAE06F` 的真机 Debug 证据显示，前台扫描、合并后数组、`@Published` 发布和 SwiftUI 观察均为 2 项，恢复 ID 均为 `68610B56`、`72EAF028`，且两张卡片都触发 `recovery_card_appeared`。因此已排除文件封口、manifest、扫描、去重、ViewModel 重建和 SwiftUI 数据发布丢失；确认阻断点是恢复卡片原先位于底部 `statusOverlay`，横向列表无垂直固有尺寸保护，并与中断提示、重新准备按钮和底部控制区竞争高度，真机上虽已创建却被压缩、裁剪或移出安全区。修复仅调整 SwiftUI 布局：恢复卡片与手动重新准备入口迁移到独立、不可压缩的恢复操作面板；常规高度纵向排列提示、横向卡片和按钮，紧凑高度并列有界卡片列表与固定可点击的重新准备按钮；第一张卡片占当前列表视口，多张仍按安全卡片边界横向滚动，不自动打开检查页。新增 UI 覆盖要求第一张卡片与重新准备按钮同时 `exists && isHittable`，并以两张恢复卡片、横屏紧凑高度和辅助功能特大字号验证第二张可横向滚动访问。Debug 另记录卡片 global frame、可见安全区及交集，Release 条件编译不含该诊断；未修改 AVFoundation、后台任务、恢复扫描/合并、manifest、去重或录制状态机。静态统计现为单元测试 261 项、UI 测试 27 项；全部 Swift 文件 Debug/Release 语法解析通过，相关生产源码按 iOS 17、Swift 6 严格并发和警告即错误完成 Debug/Release 类型检查，`git diff --check`、工程文件、Scheme XML、隐私清单和 Release 诊断字符串隔离通过。当前隔离进程仍无法连接 CoreSimulator，故 261/261 与 27/27 尚未取得本轮完整 Xcode 结果，不得记为通过。M3-07 继续为“失败／修复验证中”，须先由外部 Xcode 完整测试，再在 iPhone 16 复测竖屏、横屏、较大 Dynamic Type 和 VoiceOver 下的真实可见性与可点击性。
- M3-07B 主动后台最终化竞态真机证据（2026-08-05）：iPhone 16 日志记录 `recording_did_finish_received`，session `B016005A`、recording `EEF068C8`、lifecycle 1，且 `file_exists=true`、`file_nonempty=true`；但没有后台最终化或 manifest 提交事件。前台扫描仍显示 episode `FE706F70` 的 `recording_id=none`、`file_finished=false`、`manifest_committed=false`、`recovery_count=0`。因此确认不是 AVFoundation 文件封口、恢复扫描或 UI 布局问题，而是有效 didFinish 与 `applicationBackgrounded` episode 到达顺序竞争：旧实现没有独立保留“已封口、待判定处置”的 recording 事实，episode 后到时无法再关联该录像。
- M3-07B 双向最终化对账修复（2026-08-05，未提交、待真机复测）：从起录请求开始，以 recording UUID 为键保留 project、临时 URL、session UUID、lifecycle generation、didStart、最终文件与时长、处置类型、manifest 结算和后台到期事实，直到录像明确成为正常完成、已提交恢复片段或失败结算。`didFinish → applicationBackgrounded` 会先锁存有效媒体事实，后到 episode 再按 session/lifecycle 精确关联；`applicationBackgrounded → didFinish` 继续沿用安全停止和有限后台封口。两项事实齐备后由单一 reconciliation 屏障执行一次验证、一次原子 manifest 提交、一次去重发布；前台扫描过早时由提交成功后的直接发布与补扫收束。用户正常停止先锁存 `.normalCompletion`，不会误转为中断片段；重复、旧 lifecycle、无效文件、manifest 失败和后台任务到期均不发布虚假卡片，并持续保留“重新准备摄像头”。Debug 事件只记录 8 位 recording/session/episode ID 和 lifecycle，Release 不包含诊断字符串。
- M3-07B 本轮自动化、构建与审计（2026-08-05）：新增并强化所有事件排列、exact-once、正常停止、旧 lifecycle、无效文件、manifest 失败和后台到期边界；摄像 ViewModel 98/98 通过，无效媒体失败路径连续 10/10，同页双录 UI 在 Debug 确定性起录配置下连续 5/5。首次全量单元为 267/268，唯一失败是测试在恢复卡片发布后、后台任务结束前读取瞬时中间态；改为同时等待两项最终事实后，最终全量 268/268。首次全量 UI 为 26/27，唯一失败是同页第二次录制在高负载下仍处于与该用例无关的 3 秒 Fake 倒计时；保留独立倒计时 UI 验收，并令该连续双录用例通过仅 Debug 参数执行一个零等待逻辑倒计时步骤，定向 5/5 后第二轮完整 UI 27/27。最终失败 0、跳过 0；iPhone/iPad 模拟器 Debug、Generic Device Debug/Release 无签名构建通过；`git diff --check`、工程/Scheme/隐私清单格式、`.addOnly`、敏感信息、第三方依赖和 Release Fake/测试参数/诊断字符串审计通过。该阶段只解除软件阻断，当时仍待 iPhone 16 主动后台复测；该待办已由下一条 3/3 真机证据关闭。
- M3-07B 主动后台最终真机复测（2026-08-05）：iPhone 16 / iOS 26.5.2，测试版本 `660ad4bbc0425c07b1e37c2f4234a62e05d735d7` 加当前未提交 M3-07 修复。录制中主动进入后台 15 秒再返回连续 3/3 通过：三轮均安全停止，返回后无需再次切换前后台即自动出现且仅出现一张匹配恢复卡片；中断原因正确，片段全部可播放、有声音且方向正确；用户保留后仍明确标记为中断片段，“重新准备摄像头”持续可见，重新准备后预览恢复且不自动录制。全程无重复卡片、文件覆盖、虚假卡片、黑屏、永久准备状态或强制结束 App，异常为无。由此解除主动后台子项阻断；历史 0/3 失败、`EEF068C8` didFinish 与后台 episode 竞态及双向最终化对账修复记录完整保留。M3-07A、锁屏和主动后台子项已通过，但普通来电、FaceTime/微信切换、外部占用、系统压力与媒体服务场景仍待真机，M3-07 和模块 3 继续为“进行中”，模块 4 未开始。
- M3-14 飞行模式离线真机验收（2026-08-05）：iPhone 16 / iOS 26.5.2，测试版本 `f28d4537f0725b57dd0920cef13e00ee4a6f7cd2`，飞行模式且蜂窝网络与 Wi-Fi 均关闭，连续 3/3 通过。三轮均可离线正常启动 App，稿件列表、编辑、保存和普通提词器可用；摄像预览正常，1080p 录制可开始和停止，视频均可播放且有声音；系统分享面板可打开，关闭后本地文件仍存在，退出并重新进入摄像提词正常，无网络错误或持续加载。全程不要求登录，无云上传、第三方分析或广告提示，无黑屏、崩溃、卡死或强制结束 App，异常为无。M3-14 标记为“通过”；本轮只更新验收文档，生产和测试代码均未改变，因此不重复运行既有 268/268 单元测试和 27/27 UI 测试，引用最近有效结果。M3-07、模块 3 继续为“进行中”，模块 4 保持“未开始”。
- M3-15 视频文件与播放器兼容性真机验收（2026-08-05）：iPhone 16 / iOS 26.5.2，测试版本 `919197153d0df74f8316b328ecd3c477fec465e5`。正常 1080p 文件在 TakeFlow、照片 App 和 QuickTime 均可播放，开头、中间、结尾拖动正常；当时将该 1080p 观察记录为 HEVC、30 fps、AAC 48 kHz、1920×1080、16:9，并记录原始文件含 6 个 `moof` 分片，但没有保留足以确认受检文件来自 TakeFlow 原始分享、照片 App 副本还是旧安装包的受控取证链，因此现仅作为非受控历史观察，不据此写死原因。正常 4K 文件在三处均可播放并可在三个位置正常拖动；媒体为 HEVC、30 fps、AAC 48 kHz、3840×2160、16:9，方向、镜像和音画同步正确；从 TakeFlow 直接分享取得的原始 `DF7CF6A5…MOV` 找到 5 个 `moof` 分片。主动后台中断片段会自动出现恢复卡片并明确标记为中断片段，在 TakeFlow 和 QuickTime 均可播放，中断点附近可正常拖动，视频和音频轨道存在且有声音，方向正确且无明显音画漂移，不会冒充正常完成录像；从 TakeFlow 直接分享的原始 `E82CC853…MOV` 找到 5 个 `moof` 分片。调查历史保留：最初经照片 App 取得的 4K 与中断副本未搜索到 `moof`，改用 TakeFlow 分享的生产原始文件后均发现多个分片；该调查只说明明确比较的 4K 与中断副本可能经过照片 App 重新封装，不用于解释旧 1080p/HEVC 观察。2026-08-15 以受控取证方式复核 1080p：iPhone 16 / iOS 26.5.2，测试版本 `d6c42e49925218d140564685c54b2f2d2dc8aa94`，前、后摄原始 MOV 均由 TakeFlow 预览分享面板直接保存到“文件”，未经过照片 App；两份文件均为 1080×1920、H.264、MPEG-4 AAC 且可播放，符合“1080p 优先 H.264”，受控复核通过。M3-15 保持“通过”；本轮只更新文档，引用最近有效 268/268 单元和 27/27 UI 结果，不重复运行。M3-07、模块 3 继续为“进行中”，模块 4 未开始。
- M3-12 照片保存与系统分享组合验收（2026-08-05）：复用 M3-17 在 iPhone 16 / iOS 26.5.2 的真机证据。保存成功连续 3/3：播放中保存会真实暂停，按钮显示“正在保存…”且禁止重复点击，当前预览显示“已保存到照片”，每轮照片中只新增一份视频，App 内原始录像继续存在，再次播放仅一路声音。权限拒绝与恢复：照片权限只在主动保存时请求且仅为 add-only；拒绝后当前预览显示权限错误，App 内录像仍可播放和分享，未请求读取权限；系统设置恢复“仅添加照片”后可重试并在照片中确认视频。系统分享可打开，打开前播放器暂停，取消后保持暂停且 App 内文件仍存在，不产生重复播放器或后台声音；VoiceOver 可读取保存中、成功和权限错误。非权限类 Photos 写入失败没有在真机人为制造；本轮仅补强 `LocalRecordingPlayerControllerTests.testNonPermissionPhotoWriteFailureKeepsRecordingPreviewShareAndRetryAvailable`，通过注入 `.failed` 明确证明不显示虚假成功、原子写入的本地源文件仍存在、预览及分享边界继续可用、可重试、重复点击只发出一次重试、活动播放器上限为 1，随后注入成功可进入 saved。定向 1/1、完整单元 268/268、完整 UI 27/27 均通过，失败 0、跳过 0；生产代码未修改。基于真机成功/拒绝/分享与自动化非权限失败的组合证据，M3-12 标记为“通过”；不声称真机制造过非权限 Photos 写入失败，也未采用填满存储、破坏照片库或删除用户数据。M3-07 和模块 3 继续为“进行中”，模块 4 未开始。
- M3-07B 微信视频真机验收（2026-08-05）：iPhone 16 / iOS 26.5.2，测试版本 `430bc5c528f36d299233873873ca07d238900eae`，1080p、内置麦克风。第一组“微信视频通话期间与 TakeFlow 来回切换”连续 3/3 通过：微信摄像头和麦克风工作正常；进入 TakeFlow 前没有活动录像，因此本组不要求也未记录恢复片段；切换后不显示虚假的“摄像头被占用”或可录制状态，实际提示原文未记录；返回微信通话仍正常，再次切入 TakeFlow 无崩溃或永久准备；挂断后“重新准备摄像头”存在，用户点击后预览恢复且不自动起录，可主动完成一段可播放、有声音的新录像，三轮异常均为无。第二组“TakeFlow 录制中切换到微信视频”连续 3/3 通过：录制均安全停止，在微信停留约 15 秒不计入录像，挂断返回后不自动续录；每轮只出现一张新的匹配恢复卡片，中断原因与系统实际事件一致但提示原文未记录；所有恢复片段均可播放、有声音且方向正确，无文件覆盖或重复卡片；“重新准备摄像头”持续存在，用户点击后预览恢复且不自动起录，可主动完成新录像。全程无黑屏、永久准备、崩溃、卡死或强制结束 App。此前微信/视频应用切换曾出现麦克风占用提示后没有重新准备入口的失败历史及 interruption episode、manual-reprepare、最终化对账修复记录全部保留。FaceTime 明确为本阶段未纳入验收范围，未标记通过；M3-07 仍有普通来电、外部占用、系统压力及媒体服务真机矩阵待执行，M3-07 与模块 3 继续为“进行中”，模块 4 未开始。本轮仅更新文档，引用最近有效 268/268 单元和 27/27 UI 结果，不重复运行。
- M3-07B 普通来电快速拒接真机验收（2026-08-05）：iPhone 16 / iOS 26.5.2，测试版本 `7746c6e95a2a9902260c8a04480c2635cc0e475b`，关闭实时语音留言，连续 3/3 通过。三轮来电前 TakeFlow 均已正常录制；点击拒接后录像和计时连续增长，未显示虚假的“录制中断”或“重新准备摄像头”，也未生成恢复卡片；用户随后可正常停止，最终视频均可播放且有声音，来电前后画面连续，音频无明显异常或永久丢失，无空文件、重复文件或文件覆盖，异常均为无。结论限定为该设备与系统在此配置下未向 TakeFlow 产生可观察的录制中断，应用正确保持正常录制状态且不伪造中断产物；本项不是中断恢复片段验收。此前快速拒接曾显示类似“已保留可恢复片段”但无卡片、重入无片段且无重新准备入口的失败/不确定历史，以及后续 interruption episode 与防伪状态修复记录完整保留。该子项通过不代表接听普通来电、外部占用、系统压力或媒体服务已验证；M3-07 与模块 3 继续为“进行中”，模块 4 未开始。本轮仅更新文档，引用最近有效 268/268 单元和 27/27 UI 结果，不重复运行。
- M3-07B 普通来电接听中断真机验收（证据登记日期 2026-08-15，实际执行日期未单独提供）：iPhone 16 / iOS 26.5.2，测试版本 `036c2d8082c7c32f93702d148f6ce0ce344bcbbb`。在 TakeFlow 录制中接听普通蜂窝来电，通话约 15 秒后挂断并返回 App，连续 3/3 通过。三轮均安全停止录制，通话时间未计入录像，挂断后不自动继续；每轮只生成一张匹配恢复卡片，片段可播放、有声音且方向正确；“重新准备摄像头”持续存在，检查片段或稍后处理不会清除入口；用户手动重新准备后预览恢复但不自动起录，并可主动完成一段正常新录像。全程无重复卡片、文件覆盖、黑屏、卡死或永久准备，异常均为无。该证据与快速拒接“系统未产生中断”路径明确区分，属于真实中断后的安全封口、恢复片段和手动恢复验收；此前来电快速拒接及中断生命周期失败/不确定记录和修复过程继续保留。FaceTime 本阶段仍未纳入且未标记通过；外部占用、系统压力和媒体服务仍待真机，M3-07 与模块 3 继续为“进行中”，模块 4 未开始。本轮仅更新文档，引用最近有效 268/268 单元和 27/27 UI 结果，不重复运行。
- M3-13A 最大辅助功能字体真机失败与软件修复（2026-08-15）：iPhone 16 / iOS 26.5.2，首次测试版本 `e1df887587665828c663e3b6dc64e2dbf3167e37`，系统“更大的辅助功能字体”最大档，结果失败。稿件页面的超出内容不能完整滚动访问；摄像提词竖屏中提词正文与状态区挤压关键控制，开始、暂停、录制及预览可能不可见或不可点击，状态提示不能完整阅读；横屏控件被裁出安全区且旋转回来不能可靠恢复；恢复卡片虽发布，但“检查片段”和“重新准备摄像头”不可访问，恢复面板被底部控制区遮挡，因此本地预览真机子流程未执行。根因是顶部、提词正文、状态/恢复内容与底部操作共用一条垂直压缩链，在最大 Dynamic Type 与紧凑高度下缺少独立高度预算；稿件行和编辑器的水平度量/设置同样缺少辅助字体回退布局。软件修复将摄像页拆为固定安全区顶部、可伸缩提词区域、有界可滚动状态区、独立可滚动恢复区和固定安全区底部控制；辅助字体时以 44 pt 以上图标按钮配合完整 VoiceOver 标签，单/双恢复卡片保持横向滚动且检查按钮与重新准备入口独立可点；普通提词器固定退出和主控制，稿件列表/编辑器使用可换行与纵向回退。未改变录制状态机、AVFoundation、恢复文件语义或编码配置，也未自动打开恢复检查 Sheet。
- M3-13A 自动化与构建证据（2026-08-15，当前未提交修复）：新增最大辅助字体的稿件列表/编辑器、普通提词器和摄像提词 UI 流程，覆盖竖屏、横屏、旋转回竖屏、开始/暂停、录制/停止、预览/关闭/退出、播放器释放，以及一张和两张恢复卡片的 `exists`/`isHittable`、检查片段与重新准备入口。四项最大字体定向 UI 4/4 通过；最终完整 UI 30/30，失败 0、跳过 0。首轮完整单元 267/268，唯一失败为既有 `testDistantAnchorJumpsDoNotLayOutWholeDocument` 单次 1143.835 ms 超过 1000 ms 硬阈值；未修改实现或阈值，该项随后连续 3/3 通过，第二轮完整单元 268/268，失败 0、跳过 0。iPhone 17 Pro 与 iPad (A16) / iOS 26.0.1 模拟器 Debug、Generic iOS Device Release 无签名构建均通过；仅有 Xcode 既有 AppIntents 未链接提示，无新增 Swift 源码或并发警告。此证据只解除软件与模拟器阻断，M3-13A 仍为“失败／软件修复后待 iPhone 16 最大辅助字体复测”，不得标记通过；模块 3 保持进行中，模块 4 未开始。
- M3-13A 核心布局真机复测及残留问题（2026-08-15）：iPhone 16 / iOS 26.5.2，最大辅助功能字体，`HEAD` `e1df887587665828c663e3b6dc64e2dbf3167e37` 加未提交 M3-13A 修复。原 M3-13A 核心布局矩阵连续 3/3 通过，但同时确认两个残留缺陷：最大辅助字体下点击稿件删除入口未完成删除；摄像提词页面的音频设备控件位于画面中央，遮挡提词正文且视觉层级不合理。因此 M3-13A 继续为“残留问题修复后待真机复测”，不改为通过。
- M3-13A 残留问题软件修复与回归（2026-08-15，当前未提交）：删除服务、恢复草稿、软删除/撤销与仓储调用链检查正常，问题发生在辅助字体下缺少稳定的 44 pt 直接删除入口及系统确认界面布局不可控；现增加可滚动的项目内删除确认 Sheet，取消与确认按钮固定在安全可访问区域，失败继续通过既有错误状态展示且不移除目标稿件。音频设备控件仅做布局归位，从中央状态层移至顶部设备配置区，并以独立详情 Sheet 完整显示当前输入名称及 VoiceOver 标签、值和提示；未改变 AVAudioSession、实际路由或录音行为。另修复辅助字体专用提词器状态标识挂在 ScrollView 容器导致动态状态不可稳定读取的问题，将标识归属于实际状态文本，不改变播放状态机。新增删除失败单元测试及最大字体删除取消/确认重启、音频入口竖横屏/旋转/非中央区域/VoiceOver 属性 UI 覆盖。首次完整 UI 为 30/32：最大字体状态标识缺陷稳定复现，既有重启持久化用例受模拟器空闲查询瞬态影响；后者原样定向 3/3 通过。状态标识修复后首次三轮为 2/3，唯一失败发生在进入提词器前一次性键入 80 行测试数据时自动保存等待超时；将该布局用例造数缩至仍可滚动的 30 行且不更改任何产品断言后连续 3/3，最终完整 UI 32/32。最终完整单元 269/269；iPhone 测试构建、iPad 模拟器 Debug、Generic iOS Device Release 无签名构建均通过。模拟器不能替代真实触控、实际最大字体视觉层级和 VoiceOver 手势，仍须 iPhone 16 复测；模块 3 保持进行中，模块 4 未开始。
- M3-13A 最终真机验收（证据登记日期 2026-08-16）：iPhone 16 / iOS 26.5.2，测试版本 `e1df887587665828c663e3b6dc64e2dbf3167e37` 加未提交 M3-13A 修复，系统“更大的辅助功能字体”最大档。完整布局矩阵连续 3/3 通过；稿件删除取消、确认及重启持久化专项连续 3/3 通过；摄像提词音频设备入口在竖屏、横屏及旋转恢复专项连续 3/3 通过，完整设备名称与 VoiceOver 反馈通过。全程无按钮无反应、关键内容裁剪或遮挡，无黑屏、崩溃、卡死或强制结束 App，异常为无。M3-13A 标记为“通过”；首次最大字体失败、核心布局修复 3/3 后发现删除和音频布局残留、第二轮软件修复与最终专项证据全部保留。M3-13 整体仍因 M3-13B VoiceOver 完整矩阵未执行而保持“进行中”；模块 3 继续“进行中”，模块 4 未开始。

## 规格审查：待确认、遗漏与技术风险

下表保留规格原要求，只记录开发前需要解决的决策。除明确标为“阻断”的条目外，不代表删除或降低任何验收条件。

| ID | 类型 | 影响模块 | 问题或风险 | 建议决定 | 状态 |
|---|---|---|---|---|---|
| SPEC-001 | 已解决 | 0、9 | 用户请求中的产品名是“一遍成”，规格 1.0 的暂定名是“一遍过”。 | 已确认内部工程名 `TakeFlow`、用户可见名“一遍成”；规格原文不改写。 | 2026-07-28 已解决 |
| SPEC-002 | 范围遗漏 | 1、2、7 | 核心路径写有“导入稿件”“自动口语化和断句”“试讲并校准语速”，但模块 1 仅支持粘贴并明确排除 PDF/Word；没有文件导入或试讲校准模块；AI 口语化又属于后置 Pro 能力。 | 定义首发导入格式、断句是否本地基础能力、试讲校准流程及免费/Pro 边界。 | 待确认 |
| SPEC-003 | 范围遗漏 | 8、9 | Pro 权益包含“表现分析”，但模块 0–9 没有功能定义、数据模型、隐私边界或验收条件。 | 删除该权益文案或补充独立规格；不得自行虚构实现。 | 待确认，阻断该权益 |
| SPEC-004 | 隐私歧义 | 3、4、9 | 模块 3 必须把麦克风录入视频；模块 4 又称不得“记录或上传用户音频”。Speech Framework 在部分设备/语言下可能需要服务端识别，不能默认等同纯本地。 | 明确该禁令仅针对语音识别副本，还是也约束视频音轨；决定首版强制本地识别，或为系统/云端识别提供明确披露与同意。 | 待确认，阻断语音识别路径 |
| SPEC-005 | 定义遗漏 | 4、5、6 | “基于稿件和录制时间生成字幕”未定义时间戳来源；自由发挥、漏词、重拍和片段替换会让稿件位置与真实语音不一致。 | 定义字幕对齐算法、置信度、人工校正入口、自由发挥文本来源及失败降级。 | 待确认 |
| SPEC-006 | 权益歧义 | 4、7、8 | “有限次数语音跟随”没有计量单位和重置周期；永久买断是可选但无决定；AI 调用是否含额度未定义；离线权益的“合理”保留期不明确。 | 形成完整权益矩阵：计量事件、额度、周期、宽限期、退款/到期、永久权益、AI 成本与离线校验窗口。 | 待确认，阻断付费墙 |
| SPEC-007 | 验收不可量化 | 1、2、3、6 | “不应明显卡死”“基本一致”“不卡住”“没有明显爆音”“无可感知漂移”等没有设备、样本、时长、容差和测量方法。 | 模块 2 已批准模拟器内部阈值：1 万字符 0.5 秒、10 万字符 2 秒、普通重排主线程冻结低于 1 秒；真机阈值及模块 1、3、6 指标仍需确定。 | 模块 2 内部防退化部分解决，其余待确认 |
| SPEC-008 | 已解决 | 3、5、6 | 模块 3 启动前已批准视频、音频、镜像、方向、中断、文件、空间和照片权限矩阵。 | 默认 1080p/30 fps/H.264；能力允许时 4K/30 fps/HEVC；AAC 48 kHz；录制段方向锁定；500/250 MB 空间线；私有目录录制与 Photos add-only 分离。 | 2026-07-29 已解决 |
| SPEC-009 | 存储遗漏 | 0、1、3、5、6、9 | 未定义原片段和成片保留期、项目删除/撤销关系、崩溃遗留文件回收、临时空间预算、备份排除、用户可见存储管理。长视频可能迅速耗尽空间。 | 定义项目目录、保留与清理策略、原子提交、备份策略、低空间阈值和用户确认规则。 | 待确认 |
| SPEC-010 | 云端安全遗漏 | 7、9 | 规定客户端不存密钥，但未指定安全后端、认证、滥用防护、供应商、地域、保留/删除策略、最大稿长和隐私政策披露。 | AI 接入前批准后端架构及数据处理清单；未批准时只允许协议和测试 UI，不得声称服务可用。 | 待确认，阻断真实 AI |
| SPEC-011 | 分析隐私遗漏 | 9 | 成功指标要求匿名非内容型分析，但未指定事件定义、匿名方式、同意机制、SDK/自建方案、标识符、保留期和退出机制。 | 先定义最小事件字典与隐私方案；未批准前不采集。 | 待确认 |
| SPEC-012 | iOS 平台限制 | 3、9 | 普通 iOS App 进入后台后不能依赖摄像头继续录制。规格允许“安全停止或保存当前片段”，这是可实现路径，但不能承诺后台持续拍摄。 | 模块 3 已实现进入后台时发起一次安全停止、保留完成或可恢复文件、回前台不自动继续；真实系统调度与封口结果仍待 M3-07 验证。 | 实现完成，真机风险待验证 |
| SPEC-013 | 设备能力风险 | 3、4 | 4K、特定焦点/曝光模式、前后摄像头组合、蓝牙麦克风和本地语音识别并非所有 iOS 17 设备都支持。 | 全部能力运行时探测、隐藏不可用选项、保留固定速度与 1080p 降级，并建立设备矩阵。 | 风险已标记 |
| SPEC-014 | 性能风险 | 3、5、6 | 30 分钟 4K 录制、多片段合成、字幕烧录可能产生显著发热、存储和导出耗时；后台/锁屏也会中断导出或录制。 | 采用分阶段写入、进度与取消、热状态提示、空间预估，并在目标真机上建立基准。 | 风险已标记 |
| SPEC-015 | 上架依赖遗漏 | 8、9 | 隐私政策、使用条款、支持页面、订阅商品 ID、App Store Connect 配置、截图设备/语言和 App Review 账号尚未提供。 | 模块 8 前确定商品；模块 9 前提供有效公开 URL、商店资料和审核说明输入。 | 待外部资料 |
| SPEC-016 | 可访问性范围 | 2、3、7、8、9 | 模块 2 只要求核心按钮可识别，模块 9 又要求 VoiceOver 核心流程可操作；录制、字幕编辑、AI 差异和订阅页的操作语义尚未逐项定义。 | 把无障碍作为所有模块的横切验收，而不是最后补做。 | 风险已标记 |

## 关键决策记录

| 日期 | 决策 | 影响 | 批准人 |
|---|---|---|---|
| 2026-07-28 | 内部工程名与 Scheme 使用 `TakeFlow`；App 显示名使用“一遍成” | 模块 0、9 与所有用户可见文案 | 产品负责人（用户） |
| 2026-07-28 | Bundle ID 暂用 `com.example.takeflow.placeholder`，不设置正式开发团队 | 模块 0、8、9；真机安装和归档暂不可作为发布证据 | 产品负责人（用户） |
| 2026-07-28 | 领域模型使用 Swift 6 `Sendable` 值类型；真实持久化适配器留到模块 1，模块 0 只建立异步协议并用测试 Mock 验证契约 | Core、模块 1 及后续依赖 | 模块 0 架构决策 |
| 2026-07-28 | 模块 1 使用 `ScriptSchemaV1` 版本化 SwiftData Schema；稳定 UUID 为领域标识；生产 Store 仅本地、CloudKit 关闭；V1 发布后不回改，后续字段变更新增 Schema 版本并优先轻量迁移，语义转换使用显式 `MigrationStage` | Core/Persistence、ScriptEditor 与后续读取稿件的模块 | 模块 1 架构决策 |
| 2026-07-28 | 编辑自动保存采用可注入时钟的 600 ms 防抖；进入后台或离开编辑页时立即冲刷；保存失败保留屏幕草稿并显示用户可理解错误，不把失败状态伪装为已保存 | ScriptEditor、生命周期恢复 | 模块 1 架构决策 |
| 2026-07-28 | 在 600 ms 正式保存之外，每次编辑立即异步写入独立恢复快照；恢复文件位于 Application Support、采用原子替换与版本屏障；只有基础版本精确匹配且更新时才向用户提供恢复/保留选择，正式保存成功后清理 | Core/Persistence、ScriptEditor、崩溃恢复与后续迁移 | 模块 1 数据安全加固决策 |
| 2026-07-28 | 删除采用 SwiftData 软删除标记与 5 秒撤销令牌；窗口内恢复，窗口结束后精确永久删除目标 UUID；复制生成新 UUID | ScriptEditor、后续项目引用规则 | 模块 1 架构决策 |
| 2026-07-28 | 字数按去除空白后的扩展字素簇计数；预计时长默认 240 字符/分钟，可在 60–600 范围调整；最后阅读位置写入稿件记录并按正文长度夹紧 | ScriptEditor、Teleprompter 接口 | 模块 1 产品实现假设，待产品在后续模块确认默认语速 |
| 2026-07-28 | 模块 2 使用 SwiftData V2 轻量迁移保存每稿提词偏好；速度单位固定为逻辑点/秒；阅读位置使用扩展字素簇偏移；滚动按单调时间运行段推导，布局像素不作为持久化事实 | Core/Persistence、Teleprompter、模块 3 后续复用 | 模块 2 架构决策 |
| 2026-07-28 | 提词拖动期间使用滚动比例维护轻量近似锚点，结束时使用 TextKit 精确解析；这是为 10 万字符避免每帧全文命中布局的性能边界，不改变最终保存的 Character 锚点语义 | Teleprompter 文本桥接与长稿性能 | 模块 2 性能决策 |
| 2026-07-28 | 长稿采用后台 `TeleprompterDocument` 分块索引和原生 Collection View 虚拟化；块目标 1,500、硬上限 2,200 Character，预取前 1 后 2，富文本 LRU 上限 8；全局 Character 锚点与原状态机保持唯一 | Teleprompter 长稿性能、模块 3 复用边界 | 模块 2 性能加固决策 |
| 2026-07-28 | 模块 2 模拟器内部防退化阈值为：1 万字符首屏 0.5 秒、10 万字符首屏 2 秒、普通显示设置重排主线程冻结低于 1 秒；测试必须硬失败并记录设备/系统/方法，不能替代真机性能结论 | 模块 2 测试与发布风险记录 | 产品负责人（用户） |
| 2026-07-29 | 模块 3 录制矩阵采用默认 1080p/30 fps/H.264、能力允许时 4K/30 fps/HEVC、AAC 48 kHz；前摄只镜像预览，录制段冻结 Rotation Coordinator 输出角度；不提供 60 fps、HDR、ProRes、双摄或后台持续录制 | CameraRecording、后续片段与导出兼容性 | 产品负责人（用户） |
| 2026-07-29 | 录制采用唯一显式状态机和带 UUID 的异步事件；AVFoundation 隔离在串行服务，文件先建元数据和独立临时文件，只有完成回调成功后提交为可播放；500 MB 阻止起录、250 MB 安全停止，Photos 仅在主动保存时 add-only 请求 | CameraRecording、Core/Persistence、模块 5/6 接口 | 模块 3 架构决策 |
| 2026-07-29 | 摄像头叠加继续复用模块 2 的唯一提词状态机、全局 Character 锚点和分块虚拟化；可见块改用有界 TextKit 1 与确定高度计算，锚点精调延后一轮主 actor 并由 revision 防止过期回调，LRU 上限仍为 8 | Teleprompter、CameraRecording、长稿性能 | 模块 3 性能回归决策 |

## 更新规则

1. 开始模块时将状态改为“进行中”，并记录范围和基线测试。
2. 出现无法继续的产品或外部依赖时改为“受阻”，链接对应 SPEC 编号。
3. 每次构建和测试记录实际命令、环境、通过数、失败数；未运行不得写“通过”。
4. 所有未自动验证的行为加入 `MANUAL_TEST_CHECKLIST.md`。
5. 只有逐条验收完成、证据齐全后才能改为“已完成”。
