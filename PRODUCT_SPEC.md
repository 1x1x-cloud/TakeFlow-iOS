# 智能口播提词器产品规格与开发任务书

版本：1.0  
目标平台：iPhone、iPad  
最低系统版本：iOS 17  
技术方向：Swift、SwiftUI、AVFoundation  
暂定产品名：一遍过

---

# 一、产品定义

## 1.1 产品愿景

“一遍过”是一款面向中文口播创作者的智能提词与录制工具。

它不只是让文字自动滚动，而是帮助用户以更少的重拍次数完成一条自然、稳定、可以直接发布的口播视频。

## 1.2 核心价值

产品必须围绕以下结果设计：

1. 用户不需要背稿。
2. 用户讲话停顿时，提词内容不会继续跑掉。
3. 用户临场换词或自由发挥后，系统能够重新定位稿件。
4. 用户说错一句时，不需要重拍整条视频。
5. 录制出现异常时，不得丢失稿件和已录制片段。
6. 用户能够快速导出无水印、音画同步的视频。

## 1.3 目标用户

首发核心用户：

- 中文短视频知识博主
- 课程讲师与顾问
- 销售人员
- 房产、保险和汽车行业从业者
- 创业者与企业负责人
- 每周至少录制两条口播视频的人

## 1.4 核心使用路径

```text
创建或导入稿件
→ 自动口语化和断句
→ 调整提词显示方式
→ 试讲并校准语速
→ 语音跟随录制
→ 发现错句时分段重录
→ 自动拼接有效片段
→ 生成字幕
→ 预览并导出视频
```

---

# 二、产品原则

所有开发决策必须遵守以下优先级：

1. 录制稳定性高于新功能数量。
2. 不丢稿、不丢视频高于界面动画。
3. 本地处理优先于上传云端。
4. 用户明确授权后才能使用摄像头、麦克风或上传内容。
5. 免费用户必须能够完整录制并导出一条无水印视频。
6. AI 功能不可阻塞基础提词和录像功能。
7. 没有网络时，基础提词、录制和项目管理必须可用。
8. 不允许为了通过测试而写死返回值或绕过真实业务逻辑。

---

# 三、技术架构

## 3.1 推荐技术

- Swift 6
- SwiftUI
- AVFoundation
- SwiftData
- Speech Framework
- Vision Framework
- StoreKit 2
- XCTest
- XCUITest
- OSLog
- iCloud/CloudKit：后续可选

## 3.2 架构要求

采用模块化架构，至少拆分为：

```text
App
├── Core
│   ├── Models
│   ├── Persistence
│   ├── Permissions
│   ├── Logging
│   └── Utilities
├── ScriptEditor
├── Teleprompter
├── CameraRecording
├── SpeechTracking
├── RetakeComposer
├── Captioning
├── VideoExport
├── Subscription
└── Settings
```

要求：

- 业务逻辑不得全部写在 SwiftUI View 中。
- 摄像头、录音、语音识别和文件写入必须通过独立服务管理。
- 服务应使用协议抽象，使关键逻辑能够使用 Mock 进行测试。
- 所有异步状态更新必须符合 Swift Concurrency 规则。
- UI 状态更新必须在主线程或 `@MainActor` 中完成。
- 每个视频项目必须拥有稳定、唯一的 ID。
- 临时文件和最终文件必须明确分离。

## 3.3 数据模型

### Script

- id
- title
- content
- normalizedContent
- createdAt
- updatedAt
- estimatedDuration
- preferredFontSize
- preferredScrollSpeed
- lastReadPosition

### RecordingProject

- id
- scriptId
- createdAt
- updatedAt
- status
- orientation
- resolution
- activeSegmentId
- finalVideoURL
- captionFileURL

### RecordingSegment

- id
- projectId
- sequence
- localFileURL
- startScriptPosition
- endScriptPosition
- duration
- status
- createdAt

### UserPreferences

- defaultFontSize
- lineSpacing
- textAreaWidth
- textAreaVerticalPosition
- overlayOpacity
- countdownSeconds
- defaultResolution
- voiceTrackingEnabled

---

# 四、通用验收标准

以下标准适用于每一个开发模块。

## 4.1 代码标准

- 工程能够在当前稳定版 Xcode 中编译。
- 不得出现编译警告，确实无法消除的系统警告必须说明原因。
- 不使用已经废弃的系统 API。
- 不加入未经批准的第三方 SDK。
- 不将密钥写进源码、配置文件或 Git 历史。
- 不使用强制解包处理用户数据、文件路径或设备能力。
- 错误必须转化为用户能够理解的状态或提示。
- 关键状态变化必须记录到结构化日志中，但日志不得包含稿件正文、音频或隐私数据。

## 4.2 测试标准

- 核心业务逻辑必须有单元测试。
- 每个错误分支至少有一个测试。
- 使用 Mock 测试权限拒绝、文件失败和服务中断。
- 对无法自动化的摄像头或真机功能提供手动测试清单。
- Codex 必须实际执行可运行的测试，并报告结果。
- 不得仅凭阅读代码声称测试通过。

## 4.3 交付标准

每个模块完成后，Codex 必须输出：

1. 完成了什么。
2. 修改或新增了哪些文件。
3. 关键设计决策。
4. 实际运行了哪些构建和测试。
5. 构建及测试结果。
6. 尚未验证的真机项目。
7. 已知限制和风险。
8. 下一模块可以依赖的接口。

若构建或测试失败，不得将任务描述为“完成”。

---

# 五、模块开发提示词

## 模块 0：工程初始化与规则建立

### 交给 Codex 的提示词

你正在开发一款原生 iOS 智能口播提词器。请先完整阅读仓库根目录的 `PRODUCT_SPEC.md`，不要立即编码。

完成以下工作：

1. 检查当前仓库、Xcode 和 Swift 环境。
2. 创建或整理原生 SwiftUI 工程。
3. 最低支持 iOS 17，支持 iPhone 和 iPad。
4. 建立 PRODUCT_SPEC 中规定的模块目录。
5. 建立基础数据模型、错误类型、日志工具和服务协议。
6. 建立单元测试与 UI 测试 Target。
7. 创建 `AGENTS.md`，记录项目架构、编码规则、测试方式和不可违反的产品原则。
8. 创建 `DEVELOPMENT_STATUS.md`，用于跟踪模块状态、测试状态和已知风险。
9. 不实现摄像头和 AI 业务，只建立可扩展基础。

开始修改前，先用自己的话复述：

- 产品目标
- 核心用户
- 模块边界
- 本模块范围
- 明确不在本模块实现的内容
- 计划执行的验证

完成后必须实际执行可用的构建和测试。不要通过删除测试或降低编译要求解决失败。

### 验收条件

- 工程可以编译。
- App 可以启动并显示基础首页。
- iPhone 和 iPad Target 配置正确。
- 目录和依赖方向清晰。
- 已建立测试 Target。
- 数据模型可以创建、保存和读取。
- `AGENTS.md` 包含构建与测试命令。
- `DEVELOPMENT_STATUS.md` 已记录模块 0 状态。
- 不包含真实 API Key。
- 不包含无关第三方依赖。

---

## 模块 1：稿件管理与编辑

### 交给 Codex 的提示词

完整阅读 `PRODUCT_SPEC.md`、`AGENTS.md` 和 `DEVELOPMENT_STATUS.md`。

实现稿件管理模块，范围仅包括：

1. 创建、编辑、重命名、复制和删除稿件。
2. 自动保存，输入停止后短暂防抖保存。
3. 稿件列表搜索和排序。
4. 显示字数与预计口播时长。
5. 保存用户最后阅读位置。
6. 支持粘贴纯文本。
7. 删除操作需要确认，并提供短时间撤销能力。
8. 应用意外退出后，已经输入的稿件应能够恢复。
9. 中文、英文、数字和 Emoji 不得导致崩溃。
10. 无网络状态下完全可用。

不要实现 PDF、Word 导入、AI 改稿、摄像头或语音跟随。

开始前先复述需求并列出数据流、异常情况和测试计划。发现现有结构不合适时，可以进行必要重构，但不得破坏已经通过的功能。

### 验收条件

- 用户可创建空白稿件。
- 编辑后自动保存，无需点击保存按钮。
- 强制关闭并重新打开 App 后稿件仍存在。
- 10万字符稿件可以打开和编辑，界面不应明显卡死。
- 删除前有确认。
- 删除后可以撤销。
- 搜索支持标题和正文。
- 预计时长根据可配置语速计算。
- 稿件内容为空时有明确空状态。
- 持久化失败时有提示，不得静默丢失。
- 单元测试覆盖创建、更新、删除、撤销、搜索和恢复。

---

## 模块 2：基础提词器

### 交给 Codex 的提示词

实现基础提词器，不接入摄像头。

功能要求：

1. 全屏提词模式。
2. 固定速度自动滚动。
3. 开始、暂停、继续和重新开始。
4. 调节字号、行距、滚动速度和左右边距。
5. 调节提词区域的宽度和垂直位置。
6. 深色与浅色显示。
7. 水平镜像和垂直镜像。
8. 3、5、10秒倒计时。
9. 点击屏幕切换控制栏显示。
10. 退出后保存阅读位置和显示偏好。
11. 用户拖动文本后，应从新的位置继续滚动。
12. VoiceOver 开启时，核心控制按钮必须可识别。

滚动速度必须基于真实时间计算，不得依赖固定帧率。不同刷新率设备上的视觉速度应基本一致。

### 验收条件

- 60Hz 与高刷新率设备逻辑上使用相同滚动速度。
- 暂停后文本位置不变化。
- 继续后从原位置滚动。
- 用户手动拖动后不会跳回旧位置。
- 切换字号后尽量保持当前阅读句可见。
- 横竖屏切换不会回到稿件开头。
- 前后台切换不会异常快进。
- 镜像模式文字方向正确。
- 无障碍标签完整。
- 核心滚动状态机有自动化测试。

---

## 模块 3：摄像头与视频录制

### 交给 Codex 的提示词

实现基于 AVFoundation 的录制模块，并与基础提词器组合。

功能要求：

1. 摄像头实时预览。
2. 前后摄像头切换。
3. 麦克风录音。
4. 竖屏与横屏录制。
5. 1080p 录制，设备支持时提供 4K。
6. 点击对焦和曝光。
7. 可选焦点与曝光锁定。
8. 录制倒计时。
9. 显示录制时长。
10. 显示剩余存储空间不足提示。
11. 处理中断事件：来电、音频会话中断、进入后台。
12. 已经写入的视频片段不得因为中断而全部丢失。
13. 提词文本只显示在界面上，不能被录进最终视频。
14. 支持前置摄像头预览镜像，但导出方向必须符合用户预期。
15. 摄像头或麦克风权限被拒绝时，提供解释和进入系统设置入口。

AVFoundation 会话必须与 SwiftUI 生命周期隔离。不得在 View 中堆积全部录制逻辑。

### 验收条件

- 摄像头预览不卡住且方向正确。
- 录制文件包含视频和声音。
- 提词 UI 不出现在导出视频中。
- 前后摄像头切换不会造成 App 崩溃。
- 不支持 4K 的设备不会显示不可用选项。
- 权限拒绝后不会反复请求。
- 录制开始、进行中、停止和失败状态明确。
- 停止录制后文件可以正常播放。
- 进入后台时安全停止或保存当前片段。
- 音频中断后已经录制内容仍可恢复。
- 连续录制30分钟作为真机必测项目。
- 真机检查音画同步、发热、存储占用与设备旋转。

---

## 模块 4：实时语音跟随

### 交给 Codex 的提示词

实现中文口播稿件的实时语音跟随。第一版采用句子级跟随，不追求逐字动画。

功能要求：

1. 将稿件规范化并拆分为可追踪的句子。
2. 实时接收语音识别结果。
3. 根据已识别文本判断用户讲到哪一句。
4. 用户停顿时停止推进。
5. 用户重复上一句时不得向后错误跳跃。
6. 用户遗漏少量词语时仍能匹配当前句。
7. 用户临场替换同义表达时允许继续。
8. 用户短暂自由发挥后，能在后续原稿中重新定位。
9. 匹配置信度低时保持当前位置，不得胡乱跳转。
10. 支持用户随时切换回固定速度模式。
11. 支持普通话夹英文、阿拉伯数字和常见标点。
12. 识别不可用时，基础提词和录制仍能工作。
13. 不得记录或上传用户音频，除非产品配置明确启用并获得授权。

将文本规范化、相似度计算、候选选择和位置状态机设计成独立、可测试组件。

### 验收条件

使用固定测试语料自动验证：

- 完全照稿朗读时顺序推进。
- 漏掉一句中的少量词语时仍匹配当前句。
- 同一句重复两遍时不跳到错误段落。
- 用户停顿时位置保持。
- 用户说了稿外内容后，能在重新读稿时找回位置。
- 稿件存在重复句时，优先结合当前位置判断。
- 低置信度输入不推动稿件。
- 识别服务报错时自动降级。
- 用户手动改动阅读位置后，跟随器以新位置为基准。
- 语音跟随状态机和匹配算法具有单元测试。
- 真机测试安静、嘈杂、普通话夹英文和蓝牙麦克风场景。

### 模块 4A 决策引用（2026-08-16）

模块 4 的首版隐私、能力降级、音频所有权、匹配门控、量化验收和 4B–4H 阶段计划已在 `MODULE4_PRODUCT_SPEC.md` 冻结。关键决定为：仅使用设备端识别；用户明确开启后先创建 `zh-CN` recognizer 并检查 `supportsOnDeviceRecognition`，不支持时不请求 Speech 或麦克风权限；支持时才显示用途说明并依次请求权限，授权后重新检查本地能力与 `isAvailable`；每个请求必须设置 `requiresOnDeviceRecognition = true`，任何阶段都不得自动回退到联网识别。识别文本、候选、时间线、评分、临时锚点和最多 2 秒 PCM 只允许在单次任务的易失内存中处理，不得持久化到 SwiftData、UserDefaults、文件、日志或分析事件，任务结束或取消、中断、模式切换、页面退出时必须释放。最低 iOS 17 使用 `SFSpeechRecognizer` 抽象，不以 iOS 26 `SpeechAnalyzer` 为基础。确定性算法语料要求预期 `Character` 锚点 100% 正确；真机设备端识别保留独立量化阈值。摄像提词的 PCM 与视频/AAC 输出必须在同一 Capture Session 真机证明共存，不能安全共存时只降级固定速度。免费/Pro 只保留协议边界，计量和付费墙仍待模块 8 决策。

---

## 模块 5：分段录制与错句重拍

### 交给 Codex 的提示词

实现“一句说错，不用整条重拍”的分段录制系统。

功能要求：

1. 每次录制生成独立 RecordingProject。
2. 项目由多个 RecordingSegment 构成。
3. 用户录制时可以标记当前句说错。
4. 标记错误后安全结束当前片段。
5. 用户选择从当前句或上一句重新录制。
6. 新片段不能覆盖旧片段。
7. 用户可以预览、保留或删除片段。
8. 支持调整有效片段顺序，但不得产生重复序号。
9. App 崩溃后可以恢复未完成项目和已有片段。
10. 最终合成前检查所有片段是否存在、可读且方向一致。
11. 删除片段必须有确认，最终视频生成前不得删除源片段。
12. 磁盘空间不足时停止新录制并保护已有文件。

### 验收条件

- 一个项目可包含多个片段。
- 重录不会覆盖原片段。
- 用户可以选择新旧版本。
- 强制退出后项目能够恢复。
- 丢失文件时显示具体问题，不得直接崩溃。
- 合成前能够生成确定且可复现的片段顺序。
- 删除项目会清理所属文件，但不会误删其他项目。
- 文件清理逻辑具有测试。
- 使用临时测试视频验证片段管理和恢复流程。
- 真机完成“第一段正确—第二段说错—重新录第二段—继续第三段”的完整流程。

---

## 模块 6：视频拼接、字幕与导出

### 交给 Codex 的提示词

实现项目最终合成与导出。

功能要求：

1. 使用 AVFoundation 合并有效视频片段。
2. 保持正确方向和宽高比。
3. 保持音画同步。
4. 片段分辨率不一致时进行明确且可预测的适配。
5. 可选对切点添加极短音频交叉淡化，减少爆音。
6. 基于稿件和录制时间生成字幕。
7. 支持编辑字幕文本和时间。
8. 支持字幕烧录进视频。
9. 支持导出 SRT 文件。
10. 支持1080p，源素材允许时支持4K。
11. 导出进度可见。
12. 用户可以取消导出。
13. 导出失败不得损坏源片段。
14. 导出完成后可保存到照片或系统分享。
15. 文件名不得包含非法字符。

### 验收条件

- 两个以上片段可以正确合并。
- 视频方向正确，不拉伸、不倒置。
- 音频在切点附近没有明显爆音。
- 10分钟成片音画同步无可感知漂移。
- 字幕时间递增且不重叠。
- SRT 格式有效。
- 取消导出后清理未完成临时文件。
- 导出失败后可以重新尝试。
- 保存照片权限拒绝时仍能使用系统分享。
- 源视频不会因导出失败被删除。
- 自动化测试覆盖字幕时间线和导出状态机。
- 真机检查1080p、4K、横屏和竖屏导出。

---

## 模块 7：AI 口语化改稿

### 交给 Codex 的提示词

实现可替换服务商的 AI 改稿模块。不要将特定供应商逻辑直接写入 View。

功能要求：

1. 定义 `ScriptAIService` 协议。
2. 支持将书面稿改成自然中文口语。
3. 支持生成前三秒钩子。
4. 支持缩短到指定时长。
5. 支持生成不同语气版本。
6. 支持标注停顿、重音和情绪。
7. 修改前保存原稿。
8. 以差异预览形式展示修改结果。
9. 用户可接受全部、接受部分或放弃。
10. 网络失败、限流和服务异常时保留原稿。
11. 不在客户端代码中存储服务端密钥。
12. 明确提示用户哪些文字将被发送到云端。
13. 用户取消后不得继续覆盖稿件。
14. AI 返回空内容或异常格式时不得替换原稿。

如果尚未确定服务商，先实现协议、Mock 服务和完整 UI 流程，不得虚构可用接口。

### 验收条件

- 没有配置 AI 服务时，基础 App 正常工作。
- 原稿永远可以恢复。
- 用户拒绝结果后正文不变化。
- AI 返回空文本时不覆盖正文。
- 重复请求可以取消。
- 请求期间防止重复提交。
- 云端上传前有明确说明。
- API Key 不存在于 App 二进制可直接读取的配置中。
- 使用 Mock 覆盖成功、失败、超时、取消、空返回和部分接受。

---

## 模块 8：订阅与权限控制

### 交给 Codex 的提示词

使用 StoreKit 2 实现免费版和 Pro 版。

免费版必须支持：

- 创建和编辑稿件
- 固定速度提词
- 1080p 录制
- 无水印导出
- 有限次数的语音跟随体验

Pro 版支持：

- 无限语音跟随
- 4K 录制
- 分段重拍和自动拼接
- AI 改稿
- 高级字幕
- 表现分析

功能要求：

1. 产品信息从 StoreKit 获取，不写死价格。
2. 支持月度、年度和可选永久买断。
3. 支持恢复购买。
4. 正确处理购买成功、取消、待处理、失败和退款。
5. App 启动时更新权益状态。
6. 离线时保留最近验证过的合理权益。
7. 订阅页清楚展示价格、周期、自动续订和权益。
8. 不在首次打开 App 时强制弹出订阅页。
9. 用户完成一次核心体验后再展示升级入口。
10. 订阅服务不可用时不得阻塞免费功能。

### 验收条件

- StoreKit 配置文件下可完成测试购买。
- 取消购买不会显示错误成功状态。
- 恢复购买能够恢复权益。
- 退款或到期后权限正确更新。
- 不同套餐不会造成重复权益冲突。
- 价格和货币来自 StoreKit。
- 免费用户可完成录制和无水印导出。
- 订阅页包含恢复购买、隐私政策和使用条款入口。
- 权益判断具有自动化测试。
- 真机或 Sandbox 账号完成购买与恢复测试。

---

## 模块 9：隐私、稳定性与上架准备

### 交给 Codex 的提示词

对整个 App 进行发布前审核和加固。

工作范围：

1. 审查摄像头、麦克风、语音识别和照片权限说明。
2. 创建并校验 `PrivacyInfo.xcprivacy`。
3. 检查第三方 SDK 和数据收集行为。
4. 检查日志中是否泄露稿件、文件路径、语音内容或用户标识。
5. 检查所有临时文件的生命周期。
6. 检查后台、中断、低存储和权限拒绝流程。
7. 检查订阅页面与恢复购买。
8. 检查动态字体、VoiceOver、深色模式和横竖屏。
9. 编写 TestFlight 测试方案。
10. 编写 App Review Notes。
11. 整理 App Store 截图清单、关键词、描述和隐私说明草稿。
12. 执行全部可运行测试。
13. 不得为了发布而隐藏已知崩溃或跳过失败测试。

### 最终验收条件

- Release 配置能够归档。
- 不含测试密钥和开发服务器地址。
- 权限说明与实际功能一致。
- 首次拒绝权限后 App 不崩溃。
- 无网络时基础提词和录制可用。
- 录制失败不会丢失稿件。
- 导出失败不会删除源片段。
- 免费用户能够完成核心流程。
- 购买恢复入口可见。
- 隐私政策和使用条款入口有效。
- iPhone 小屏、标准屏和大屏完成测试。
- 至少一台 iPad 完成测试。
- 横屏和竖屏完成测试。
- VoiceOver 核心流程可操作。
- 全部自动化测试结果已记录。
- 所有未自动验证的项目已进入真机测试清单。
- `DEVELOPMENT_STATUS.md` 与实际状态一致。

---

# 六、每次交给 Codex 的统一前置提示

将以下内容放在每个模块提示词前面：

你不是在制作演示原型，而是在开发准备提交 App Store 的生产级 iOS 应用。

在修改代码前必须：

1. 完整阅读 `PRODUCT_SPEC.md`。
2. 完整阅读 `AGENTS.md`。
3. 完整阅读 `DEVELOPMENT_STATUS.md`。
4. 检查现有代码和测试，不要假设某个功能不存在。
5. 查看当前 Git 状态，保留用户已有修改。
6. 用自己的话复述本次需求。
7. 列出本次范围和明确不做的内容。
8. 列出涉及的状态、数据流和失败场景。
9. 给出实施计划和验证计划。
10. 确认理解后直接开始，不要只输出方案。

实施过程中：

- 优先修复因本模块导致的编译和测试失败。
- 不得删除已有功能来简化任务。
- 不得绕过权限、签名或安全机制。
- 不得使用假数据冒充真实功能完成。
- 不得把敏感密钥写入客户端。
- 发现规格冲突时，停止相关实现并明确报告冲突。
- 发现合理的小型实现细节缺失时，采用最安全、最符合苹果平台习惯的方案，并记录假设。

完成前必须：

1. 构建工程。
2. 运行相关测试。
3. 检查新增代码是否存在明显并发、内存和文件安全问题。
4. 更新 `DEVELOPMENT_STATUS.md`。
5. 将未验证的设备行为列入真机测试清单。
6. 根据逐条验收条件给出“通过、失败或未验证”，不得笼统声称全部完成。

---

# 七、Codex 最终回报格式

每个模块结束时，严格使用以下格式：

## 完成结果

说明模块是否真正完成。

## 实现内容

列出已实现的用户可见功能和底层能力。

## 文件变化

列出关键新增和修改文件及用途。

## 架构决策

说明重要接口、状态管理、持久化和错误处理方式。

## 构建验证

列出实际执行的构建操作和结果。

## 自动化测试

列出实际执行的测试、通过数量和失败数量。

## 验收结果

逐条列出：

- 验收项
- 状态：通过 / 失败 / 未验证
- 验证证据
- 若未通过，说明原因

## 真机测试清单

列出必须由开发者在 iPhone 或 iPad 上执行的步骤和预期结果。

## 已知限制与风险

不得隐藏未完成项或不确定性。

## 下一步

说明下一个模块可以使用的接口，以及开始下一模块前需要解决的问题。

---

# 八、发布阻断条件

出现以下任意问题时，不得提交 App Store：

- 录制过程可能无提示丢失视频。
- App 异常退出可能丢失稿件。
- 导出视频存在稳定的音画不同步。
- 权限拒绝导致崩溃或死循环。
- AI Key 被写入客户端。
- 用户稿件或视频未经明确同意上传。
- 无法恢复购买。
- 免费版无法完成一次核心体验。
- Release 构建失败。
- 隐私清单与实际收集行为不一致。
- 关键自动化测试失败。
- 真机连续录制测试未完成。

---

# 九、产品成功指标

首发版本重点记录以下匿名、非内容型指标：

- 创建第一份稿件的比例
- 开始第一次录制的比例
- 成功导出第一条视频的比例
- 从开始录制到成功导出的耗时
- 每个项目的重录次数
- 语音跟随失去位置的次数
- 分段重拍功能使用率
- 7日内再次录制的比例
- 每周成功导出至少两条视频的用户数

不得收集稿件正文、音频内容或视频内容用于分析，除非用户主动选择参与并获得清晰说明。
